# Handoff — rm-538f80e80c2c4985: Capture attachment filename colon fix

**Agent**: hermes-local-20260921T164700Z-b11f7cec
**Completed**: 2026-09-21T18:58:00Z
**Branch**: `agent/rm-538f80e80c2c4985/fe8bdeec5b16`
**Commit**: `009ef0b`

## Acceptance criteria

1. `generateAttachmentFilename` produces filenames with no literal colon character.
2. The timestamp segment in the filename is exactly 19 characters (digits and dashes only,
   from an ISO-8601 `YYYY-MM-DDTHH:MM:SS.mmm` source string with all `:` and `.` replaced by `-`).
3. A regression test exercises `appendToInboxNote` with the full bridge mock and asserts
   the written relative path contains no colon and matches the expected filename pattern.

## Regression evidence

- Pre-fix: `generateAttachmentFilename` used `iso.replace(':', '-').replace('.', '-')`
  (two `.replace()` calls with string arguments, no `/g` flag). For `16:19:24.123`,
  this produced `1619:24123` — the seconds colon survived and the timestamp was 15 chars.
- Post-fix: single `.replace(/[:.]/g, '-')` call produces `161924123` — 9 chars, no colon.
  Full ISO string `2026-09-21T16:19:24.123` → `2026-09-21T16-19-24-123` (19 chars).

## Test results

- `npx vitest run src/capture/captureCommit.test.ts` — **12/12 pass**
- `npx vitest run` (full suite) — **2773/2773 pass, 146 test files**
- `npx tsc --noEmit` — **clean**
- `npx eslint src/capture/captureCommit.ts src/capture/captureCommit.test.ts` — **clean**

## Self-review findings

1. The previous session's test imported `copyAttachmentsToWorkspace` (a non-exported
   function) with a wrong signature (`workspaceRoot`/`noteName`/`noteContent` instead of
   the actual parameters). This test never ran and the import would have failed.
   The rewritten test correctly exercises `appendToInboxNote` through the public API
   with the full `tauriBridge` mock surface and a `rebuildLinkIndex` stub.
2. The previous session's test regex expected a dot in the timestamp (`\d{2}\.\d{3}`),
   which contradicted the implementation that uses `.replace('.', '-')`. The fix is
   consistent with the implementation (all colons and dots → dashes).
3. The previous session left the ROADMAP with a duplicate entry (claimed + unclaimed seed
   both referencing the same ID). Deduplicated in commit `2837f19`.
4. The previous session's CI run (run 35627071995) failed on exactly this test import.

## Landed SHA

- Dedup: `2837f19` → main
- Fix + test: `009ef0b` → `agent/rm-538f80e80c2c4985/fe8bdeec5b16` (pending push to main)

## CI state

- Previous CI run 35627071995: FAILED (test import error, this session's fix resolves it).
- New CI run for `009ef0b`: expected to pass after push.
