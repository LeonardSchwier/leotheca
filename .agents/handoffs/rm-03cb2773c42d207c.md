Task: rm-03cb2773c42d207c -- Maintenance review: `==highlight==` syntax (rm-c2a2c2d840b60a2e) dual-surface parity
Owner/token: Claude-Code-cloud-scheduled-kindbardeen-20260923T190102Z-802eb1e1 / 3874838d522ca0754cbac657cb272d40 (released on finish)
Scope: read-only review, touching only ROADMAP.md#rm-highlight-review and .agents/handoffs (no application code changes). Resource: highlight-review.

Reviewed: commit 52f0d979c38998f0b863693b959685fdabe71a66 (rm-c2a2c2d840b60a2e, "feat(markdown): ==highlight== syntax with emoji color codes"), the sole code commit in the session that just landed the `==highlight==` feature. Current-main baseline at review start: 1d271e7 (claim commit; feature landed at aa02da8, two commits earlier). Files examined: `src/markdown/highlight.ts` (+ its test), `src/editor/MarkdownPreview.tsx` (the `highlight` `marked` inline extension + its tests), `src/editor/livePreview.ts` (the `highlightMarkdownExtension` `@lezer/markdown` inline extension + its tests), `src/editor/MarkdownEditor.tsx` (wiring), `src/app/App.css` and `src/styles/theme.css` (color tokens/classes, light+dark). Checked `.agents/handoffs/rm-c2a2c2d840b60a2e.md` and ROADMAP.md's own entry first to avoid re-deriving already-recorded context; no other handoff or "Maintenance review:" roadmap entry covers this area yet (it landed minutes before this review started).

What was examined, and why no defect was found:

- **Shared detection logic** (`highlight.ts`): `detectHighlightColor`'s codepoint-based emoji check correctly handles astral (surrogate-pair) emoji via `codePointAt`/`fromCodePoint`, an unpaired high surrogate does not throw, and the "strip only when real content remains" rule is correct and tested.
- **Preview regex vs. editor delimiter scanner parity**: traced both `HIGHLIGHT_INLINE` (marked, regex-based) and the lezer `Highlight`/`HighlightMark` delimiter parser by hand for: triple-equals runs (`===`, `====`), adjacent highlights on one line, unterminated `==`, comparison-like `x == y`, and content beginning/ending in `=`. Both surfaces resolve identically in every case traced.
- **Empirically reproduced three specific two-surface-disagreement hazards** (the exact class of bug this feature's own design explicitly guards against) using real component/decoration tests against the actual code (via a temporary, unpublished probe test file, deleted after use, never committed): `==foo\n\nbar==` (cross-paragraph: no highlight in either surface), `==foo\nbar==` (single multi-line paragraph: highlighted as one span in both surfaces, identical text), and `- ==foo\n- bar==` (cross-list-item: no highlight in either surface). All three matched between Preview and the editor.
- **Code-span containment**: confirmed via the same kind of temporary probe that `` `==not a highlight==` `` produces no live-decoration mark in the editor (the existing MarkdownPreview.test.tsx already covers the Preview side of this case); both rely on backtick code-span parsing taking precedence over the `=`-triggered delimiter/tokenizer, consistent with CommonMark's own code-span-binds-tightest rule.
- **Sanitization**: the whole rendered document (including any `<mark>`) goes through one `DOMPurify.sanitize` call structurally, not textually, so nesting user-controlled inline HTML inside a highlight is sanitized the same as anywhere else; `class` values on `<mark>` are always one of 5 fixed literal strings from `HIGHLIGHT_COLOR_EMOJI`'s values, never interpolated from user text, so there is no class-name injection surface. Verified with the existing `<script>` sentinel test.
- **Theming**: `--color-mark-*` tokens are defined in both the `prefers-color-scheme: dark` block and the `[data-theme="dark"]` block in `theme.css`, matching the file's existing token pattern; CSS specificity between `.lt-highlight`/`.cm-live-highlight` and their `-<color>` siblings resolves correctly via source order (equal specificity, color rule declared after the base rule).
- **Disclosed scope gap re-confirmed, not a defect**: `src/export/exportNoteHtml.ts` reuses the already-rendered Preview HTML (including any `lt-highlight` markup) for standalone export, but `src/export/printNote.ts`'s stylesheet builder defines no `mark`/`highlight` CSS at all, so an exported/printed highlight falls back to the browser's plain default `<mark>` style, uncolored. This matches the landing handoff's own explicit disclosure ("export/print stylesheet's color classes" out of scope for this slice) -- not a new finding.

No defect found in this scope. The feature's dual-surface design, sanitization, and theming hold up under the boundary cases most likely to diverge between a regex-based and a delimiter-scanner-based renderer.

Checks run: read-only source inspection plus three temporary, unpublished vitest probe files (not committed, deleted after use) exercising `buildLiveDecorations`/`highlightMarkdownExtension` and `<MarkdownPreview>` directly against real `@codemirror`/`marked`/`DOMPurify` code, no mocking of the behavior under test. No application code was modified; no regression test was added since no regression was found. `git status --porcelain` confirmed clean before this handoff/finish.

Next unreviewed range: none identified as a natural continuation of this exact slice. A future review could look at the still-undisclosed-as-tested "highlight inside a table cell" and "highlight inside a blockquote" cases, or the deliberately-deferred formatting-submenu color-picker UI once that lands, but neither is a known or suspected defect today.

Retry: not applicable (finished, not blocked).
