# Handoff: rm-192c8b5d5aca77e4 — Android `SpeechRecognitionPlugin` never registered

## Session
- Agent: Claude-Code-cloud-scheduled-kindbardeen-20260923T122313Z-801d5087
- Date: 2026-09-23
- Claim: rm-192c8b5d5aca77e4 (claimed 12:49:25Z)
- Work branch: `agent/rm-192c8b5d5aca77e4/08fda477ff56`

## Context

Found while fixing `rm-aafb783f25b67c4e` ("Print/export a note on Android"):
`PrintExportPlugin` had the identical bug (never registered with the
Capacitor bridge), which turned out to be the real reason that item stayed
open despite extensive prior code, unit tests, and even a real Android SDK
build in earlier sessions. See `.agents/handoffs/rm-aafb783f25b67c4e.md`
for the full diagnosis and the evidence against the real
`node_modules/@capacitor/android/.../Bridge.java` source proving there is
no plugin auto-discovery scanner.

`SpeechRecognitionPlugin.java` (`@CapacitorPlugin(name = "SpeechRecognition")`)
has the same gap: `MainActivity.onCreate()` never called
`registerPlugin(SpeechRecognitionPlugin.class)`. This plausibly explains
`rm-b79c5d31dd1506ca`'s prior maintenance-review finding ("Android
speech-to-text never actually starts the native `SpeechRecognizer`") —
that session fixed the JS-side call logic, but if the plugin was never
reachable from the bridge, the call would reject before ever entering
`SpeechRecognitionPlugin.java`, looking exactly like a silent no-op from
the JS side.

## What was done

- `android/app/src/main/java/com/leonardschwier/leotheca/MainActivity.java`:
  added `registerPlugin(SpeechRecognitionPlugin.class);` next to the
  existing `WorkspaceMutationPlugin`/`PrintExportPlugin` registrations.

## Verification on this exact tree

| Check | Command | Result |
|-------|---------|--------|
| TypeScript | `npx tsc -p tsconfig.json --noEmit` | clean |
| Lint | `npm run lint` | clean |
| Version consistency | `npm run check-version` | pass |
| Targeted tests | `npx vitest run src/workspace src/speech src/app` | 700/700 pass |
| Full frontend tests | `npx vitest run` | 2881/2881 pass (152 files) |
| Frontend build | `npx vite build` | succeeds |
| Rust format | `cargo fmt --all -- --check` (src-tauri) | pass |
| Rust lint | `cargo clippy --all-targets -- -D warnings` (src-tauri) | pass |
| Rust tests | `cargo test` (src-tauri) | 85/85 pass |
| Rust check | `cargo check` (src-tauri) | pass |

No Android SDK bootstrapped this session (relied on the same source-level
verification against the real Capacitor bridge used for the sibling fix).
Pushed to the work branch first so hosted CI's real-SDK `android` job
(`./gradlew testDebugUnitTest` + `assembleDebug` + real emulator install)
supplies that evidence. See CI run
https://github.com/LeonardSchwier/leotheca/actions/runs/35863385228 for
the result at SHA `c05260e9158651e309771c600be39088876ab2fc`.

## Genuinely NOT verified

- A real SDK compile of this exact change locally (branch CI's
  `assembleDebug` is the real compile evidence).
- On-device confirmation that tapping the dictation button actually starts
  real speech recognition and returns a transcript. No physical or
  emulated Android device with UI access is available anywhere in this
  pipeline; CI's `android` job only installs the APK and checks the
  package path, it does not exercise the dictation UI.

## Disposition

Landing this fix to `main` if hosted CI comes back green (real, plausible
bug fix, well-verified as far as this sandbox and hosted CI allow), but
leaving the item `⏸` (blocked) rather than `✅`: on-device confirmation
that dictation now actually works remains unavailable. A future session
with Android device/emulator UI access should confirm end-to-end, then
finish this item.
