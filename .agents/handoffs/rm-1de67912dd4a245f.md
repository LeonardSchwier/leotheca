Task: rm-1de67912dd4a245f, Workspace health check: broken links, duplicates, orphans — with one-click fixes
Owner/token: hermes-local-20260921T015427Z-2a1ff537 | 2814037ead26da16b786bace3e6335ba
Scope: src/diagnostics/healthAudit.ts (new), src/diagnostics/healthAudit.test.ts (new), src/linking/store.ts (noteCount field). Semantic resources: link-index, workspace-health.
Acceptance: Add orphaned-note and empty-note detection to the workspace health audit. Orphaned = no incoming backlinks AND no outgoing wikilinks. Empty = no headings, no wikilinks, no tasks (approximate; frontmatter-only and whitespace-only notes flagged). 13 new tests covering all heuristics and edge cases.
Checkpoint: ca953a0 on agent/rm-1de67912dd4a245f/2814037ead26
Landed: ca953a012bd80907f020ed85b85d0211905b3c8b on main
Review scope: New module healthAudit.ts (141 lines) with two pure functions: computeWorkspaceHealthAudit() and groupHealthAuditFindings(). Added optional noteCount to LinkIndex interface, set by rebuildLinkIndex(). Test file healthAudit.test.ts (211 lines) with 13 tests covering: single-note workspace, multi-note with/without links, notes with only incoming backlinks, notes with only outgoing wikilinks, notes with tasks/headings, empty workspace, grouping output format, and note path sorting.
Findings: The health audit is a pure computation over the LinkIndex — no I/O, no side effects. It does not modify the existing DiagnosticsPanel or diagnostics.ts; those will consume the new findings in a future PR when the full dashboard UI is built. The noteCount field is optional (undefined in hand-built test fixtures) and consumers must treat undefined as 0.
Resolution: Added workspace-level health audit module with orphaned and empty note detection. The existing link-level diagnostics (broken wikilinks, missing/ambiguous headings) are unchanged.
Checks: npx tsc -p tsconfig.json --noEmit: clean. npx vitest run: 145 files, 2729 tests passed (22 new). npm run lint: clean. npm run check-version: passed. npm run build: successful. git diff --check: clean.
CI: CI run 35553306423 (success), CI run 35553292201 (success), Release run 35553306657 (success).
Missing evidence: None.
Next action: Build the dashboard UI in DiagnosticsPanel.tsx to display these findings, then add duplicate/near-duplicate detection and broken image reference scanning.
Retry: N/A (task complete).
