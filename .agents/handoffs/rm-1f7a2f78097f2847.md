Task: rm-1f7a2f78097f2847 -- Renaming or creating a note/folder with a name containing "/" silently diverges by platform instead of erroring identically
Owner/token: released (finished)
Scope: src/workspace/fileTreeStore.ts, src/workspace/fileTreeStore.test.ts; resource workspace-entry-naming
Acceptance: createNote/createFolder/renameEntry reject a name containing "/" or "\" before any bridge call, identically on both platforms.
Checkpoint: agent/rm-1f7a2f78097f2847/6447f92750b8 at b145654 (also the landed commit)
Landed: b145654 on main (fast-forward from claim 472a6bf)
Checks: this exact tree -- `npx tsc --noEmit` clean; `npx vitest run` 2149/2149 passing (114 files, +3 new, no regressions); `npx eslint .` 0 errors/1 pre-existing unrelated warning (FileTree.tsx); `npm run check-version` pass; `npx vite build` pass; from src-tauri (no Rust touched, health check): `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` clean, `cargo test` 62/62 passing, `cargo check` clean.
Review: traced the same user action (type a name with "/", submit) through both platform bridges. Desktop's `resolve_within_workspace` + `fs::create_dir_all(parent)` genuinely creates a missing subfolder and moves/creates the entry there -- a real, working cross-directory operation, just an unintended and undocumented one. Android's `renameWorkspacePathNoReplace` (capacitorBridgeImpl.ts) resolves the native `parentUri` from `pathDirname(fromPath)`, the source's own directory, and passes only `pathBasename(toPath)` as the new display name to `FolderAccess.renamePathNoReplace` -- SAF's rename has no path-based move semantics, so the typed directory component is silently dropped and the entry is renamed in place. Neither side surfaces an error. Fixed by rejecting the input itself (`assertValidEntryName`) before it reaches either bridge, at the three call sites that take a freely-typed name; `createNoteQuick`/`createNoteFromTemplate` derive their names from an existing file's own basename and were already safe, confirmed by reading them, not assumed.
CI: hosted run for b145654 not yet observed at finish time; a later session should confirm its conclusion and fix any real failure under this same claim.
Missing evidence: hosted CI conclusion for b145654 not yet observed. No real on-device Android confirmation (this cloud sandbox has no physical Android device); grounded in a direct reading of capacitorBridgeImpl.ts's real renameWorkspacePathNoReplace implementation and the native FolderAccessPlugin/WorkspaceMutationPlugin contract it calls, not assumed.
Next action: check hosted CI's conclusion for b145654; fix any real failure it surfaces.
Retry: available now if a later session finds that run red.
