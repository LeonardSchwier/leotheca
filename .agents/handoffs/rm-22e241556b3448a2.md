Task: rm-22e241556b3448a2 -- Maintenance review: search query parser splits a quoted phrase containing a literal " OR " on the operator
Owner/token: ClaudeCode-cloud-scheduled-kindbardeen-20260910T181243Z-57b2328d / 1b235061d1a3a9b4a8f8a59c627d49f8
Scope: src/workspace/searchQuery.ts, src/workspace/searchQuery.test.ts; resource search-query-parser
Acceptance: parseSearchQuery's OR-group split is quote-aware, so a literal " OR " inside a quoted phrase is never mistaken for the operator; regression tests cover a lone quoted phrase containing " OR " and a real OR where one side is such a phrase; existing OR/AND/quote tests unchanged.
Checkpoint: none needed; implemented, verified, and landing directly in one commit
Landed: fac4891a448e4ad85592d0fc932ed39daa753295 on main (fast-forward from claim 88b94c2)
Checks: this exact tree -- `npx tsc --noEmit` clean; `npx vitest run` 2137/2137 passing (114 files, +2 new, both revert-confirmed to fail against pre-fix code via `git stash` of the fix, restored after); `npx eslint .` 0 errors/1 pre-existing unrelated warning (FileTree.tsx); `npm run check-version` pass; `npx vite build` pass; from src-tauri (no Rust touched, health check, after apt-installing this cloud sandbox's missing libwebkit2gtk-4.1-dev/libgtk-3-dev/librsvg2-dev/libayatana-appindicator3-dev): `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` clean, `cargo test` 60/60 passing, `cargo check` clean.
Review: parseSearchQuery previously regex-split the raw query on /\s+OR\s+/ before ever tokenizing, so a quoted phrase's literal " OR " (e.g. `"foo OR bar"`) was torn apart mid-phrase despite the function's own doc comment claiming otherwise. Root cause traced to the split running on the raw string instead of on the already quote-aware `tokenize()` output. Fixed by tokenizing first, then splitting that token list on an exact "OR" token; verified the fix does not change any of the existing OR/AND/prefix/negation/quote test cases (all still pass unchanged).
CI: GitHub Actions run 34513813185 (CI workflow) and 34513813656 (Release workflow) both queued for this SHA at commit time; a later observation in this same session (or the next worker) should confirm both conclude green.
Missing evidence: none beyond hosted CI's own conclusion for this SHA, which was still queued when this handoff was written; full local verification suite (the same suite this repo's own policy treats as sufficient for direct landing without waiting) is exhaustive and green.
Next action: `finish` this item once hosted CI is confirmed green for fac4891a448e4ad85592d0fc932ed39daa753295, or note the real failure and fix it under this same claim if CI turns up something local verification missed.
Retry: available now
