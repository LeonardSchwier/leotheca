Task: rm-2a74d1a0d23d3165, Maintenance review: heading-link copy failure is silently swallowed
Owner/token: ClaudeCode-cloud-scheduled-kindbardeen-20260916T231603Z-8f88b156 | b4d6e53e5e89a757b12e65c13abe17b6
Scope: src/outline/HeadingLinkActions.tsx; src/outline/headingLinkOperations.ts. Semantic resource: heading-link-actions.
Acceptance: A rejected `navigator.clipboard.writeText` from the "Copy link" action never becomes an unhandled promise rejection; the user/screen-reader gets an explicit failure announcement instead of the button silently staying "Copy link" with no feedback; the existing success path (button text, announcement, auto-revert timer) is unchanged.
Checkpoint: agent/rm-2a74d1a0d23d3165/b4d6e53e5e89 (implementation branch, pushed alongside main landing)
Landed: pending push in this session; recorded here before the finish transaction per roadmap-workflow.md step 5-6 ordering.
Review scope: Single unreviewed component (`HeadingLinkActions.tsx` had zero commits/tests since its creation, confirmed via `git log --diff-filter=AM --name-only` across full history) found via a targeted sub-agent scan of `src/outline`, `src/tags`, and other small modules for a real, execution-confirmed defect (not a hunch).
Findings: `handleCopy` awaited `copyHeadingLink` (a bare `navigator.clipboard.writeText`) with no `try`/`catch`, called from `onClick={() => void handleCopy()}`. A rejected clipboard write (denied permission, unfocused document, restrictive WebView — all real, reachable conditions on Desktop/Android) became an unhandled promise rejection: the button silently stayed "Copy link" and `announceOutline` (the shared polite live region success already uses) was never called, so a screen-reader user got zero feedback that the action failed.
Resolution: Wrapped the `copyHeadingLink` call in `try`/`catch`. On failure: announce "Couldn't copy link to {heading}." via the existing shared `announceOutline` channel and return before setting `copied`/scheduling the revert timer, so the button never claims success it didn't achieve. Success path is untouched.
Tests: New `src/outline/HeadingLinkActions.test.tsx` (3 tests): (1) a rejected clipboard write produces no `unhandledrejection` window event and announces the failure message — revert-confirmed to fail without the fix (ran against the pre-fix tree: `unhandled` fired, Vitest reported 2 unhandled-rejection errors, the announcement assertion failed with `outlineAnnouncement.value` still `null`); (2) the button still reads "Copy link" (never a stuck "Copied") after a failure; (3) the pre-existing success path (button text "Copied", announcement "Copied link to Section one.") is unchanged.
Checks (this exact tree, after apt-get installing libwebkit2gtk-4.1-dev/libgtk-3-dev/librsvg2-dev/libayatana-appindicator3-dev/build-essential per CI's own apt line): `npx tsc -p tsconfig.json --noEmit` clean; `npm run lint` 0 errors/0 warnings; `npm run check-version` pass; `npx vitest run` 2431/2431 passing (up from 2428, +3 new, no regressions); `npx vite build` pass; `cargo test` (src-tauri) 77/77 passing (unchanged, no Rust files touched); `cargo clippy --all-targets -- -D warnings` clean; `cargo check` clean.
Review: Traced success and failure paths through the caller (`onClick`), the pure clipboard call, and the shared announcement/UI state (`copied`, `timerRef`). No data-loss/security implication (clipboard-only). Async lifecycle: the failure path returns before scheduling `timerRef`, so no dangling timer/no doubled announcement. Platform parity: `navigator.clipboard` is the same API surface on Tauri desktop and Android WebView, so the fix applies uniformly; no platform-specific bridge involved. Self-reviewed; no independent agent review used for a fix this small and localized.
CI: Not yet checked for the landed SHA at handoff-write time; full local verification suite is green, so per verification-suite.md's decision table this lands as "all applicable local checks and review pass; no known code failure" (push directly to main, hosted CI optional but should still be inspected once available for this SHA).
Missing evidence: None known for the changed behavior. No real-device confirmation needed (pure JS clipboard API, fully covered by jsdom-based tests exercising the actual rejection path).
Next action: None outstanding for this item once finished. A related but distinct, unclaimed defect was noticed but intentionally left out of scope (per the "do not bundle unrelated findings" rule): `src/workspace/FileContextMenu.tsx`'s `copyRelativePath` has the identical bare `await navigator.clipboard.writeText(...)` with no `try`/`catch`. A future maintenance-review session could seed and claim that as its own Open Bug.
Retry: Available now (item finished in this session; see ROADMAP.md `## Implemented`).
