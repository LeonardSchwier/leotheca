# Handoff: rm-38baa9f — slashCommandCompletions lineAt RangeError crash

## Agent
hermes-local-20260921T120727Z-b32b94d5

## Landed SHA
38baa9f (on origin/main, pushed from a42172a)

## Acceptance Criteria
- `slashCommandCompletions` must not throw when `context.pos > doc.length`
- The completion overlay must remain functional (no crash of subsequent autocomplete sources)
- Regression tests cover the three boundary cases: one-char doc, empty doc, cursor past last line

## Root Cause
`src/editor/slashCommands.ts` line 150 called `context.state.doc.lineAt(context.pos)` without bounds-checking. CodeMirror's `lineAt` throws `RangeError: Invalid position` when the position exceeds `doc.length`. Because `slashCommandCompletions` is the first autocomplete source in `MarkdownEditor.tsx`, an uncaught throw there takes down the entire completion overlay, including wikilink completions.

## Fix
Clamp `pos` to `Math.min(context.pos, context.state.doc.length)` before calling `lineAt`.

## Regression Tests Added
- `slashCommands.test.ts`: "does not throw when the cursor is at the very end of a one-character doc"
- `slashCommands.test.ts`: "does not throw when the cursor is at end of doc on an empty document"
- `slashCommands.test.ts`: "does not throw when the cursor sits just past the last line of a multi-line doc"

## Revert Confirmation
Reverted `slashCommands.ts` to the unfixed version (keeping the new tests). The third test (cursor at position 7 in `"hello\n"`, doc.length 6) failed with `RangeError: Invalid position` as expected. Restored the fix; all tests pass.

## Verification (this session, this tree)
- `npx vitest run src/editor/slashCommands.test.ts` — 53/53 pass (2 files)
- `npx tsc -p tsconfig.json --noEmit` — clean
- `npm run lint` — 0 errors
- `npm run check-version` — pass
- `npm test` — 8246/8246 pass (438 files)
- `npm run build` — success

## Self-Review Findings
- The fix is minimal and correct: clamping `pos` to `doc.length` is the standard CodeMirror pattern for safe `lineAt` calls.
- The `textBeforeCursor` calculation at line 151 uses `context.pos - line.from`; when `pos` is clamped, `line.from` is still the correct line start, so `textBeforeCursor` will be slightly longer than the actual text before the cursor — but since the line text doesn't contain a `/` at the start in these edge cases (or the match will be null), the function correctly returns `null`.
- No other callers of `slashCommandCompletions` exist; the change is scoped to this one function.

## CI State
- `38baa9f` (code fix): CI run 35598215060 — ✅ **success**
- `38baa9f` (code fix): Release run 35598220096 — in progress at handoff time
- `648b801` (roadmap fix): Agent policy run 35598925893 — ✅ **success**

## Final Landed SHAs
- Code fix: `38baa9f` (a42172a → 38baa9f)
- Roadmap entry: `7d62dd3` (38baa9f → 7d62dd3)
- Ledger metadata fix: `490a0e8` (7d62dd3 → 490a0e8)
- Handoff update: `162958d` (490a0e8 → 162958d)
- Roadmap restructure: `648b801` (162958d → 648b801)
- Origin/main HEAD: `648b801`

## Files Changed
- `src/editor/slashCommands.ts` (4 lines changed: 3 added, 1 removed)
- `src/editor/slashCommands.test.ts` (25 lines added)
