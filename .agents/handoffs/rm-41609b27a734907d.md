# Handoff — rm-41609b27a734907d (Mermaid: untested async render path and unexported sanitizer)

Agent: hermes-local-20260922T021329Z-438edb42
Item: rm-41609b27a734907d
Token: 5cdf7779450bac0d2f8be78b66f4d406
Work branch: agent/rm-41609b27a734907d/5cdf7779450b
Claimed: 2026-09-22T02:18:41Z
Lease until: 2026-09-22T03:52:38Z (renewed 02:22:38Z)

## Task
Cover the previously untested async render path and sanitizer in `src/markdown/mermaid.ts`.

## Acceptance criteria (from roadmap item)
1. `sanitizeMermaidSvg` is exported from `src/markdown/mermaid.ts` with at least two unit tests:
   a. a malicious `onerror`/`onclick` attribute is stripped,
   b. a `javascript:` or `data:text/html` href is rewritten to `#`.
2. A test exercises `renderMermaidToSvg`'s fallback path (mermaid.render rejects/throws or
   invalid source) and asserts the returned HTML is a `<pre><code class="language-mermaid">`
   block containing the original source, not an empty string or an unhandled rejection.
3. `npx vitest run` (full FRONTEND_CHECK suite) passes.
4. No change to `MarkdownPreview.tsx`'s effect or to the smoke test's existing assertions.

## Recovery note
Prior session (hermes-local-20260922T003311Z-a1afdc95, token c883b9723e29) claimed the same
item and had partial work in the dirty tree (mermaid.ts export + tests). That session crashed
due to local model endpoint failure (HTTP 500) and never committed or pushed. This session
recovered the work, cleaned the bad changes from the `agent/rm-mermaid-a1afdc95` branch merge,
and completed the implementation.

## Implementation
- `src/markdown/mermaid.ts`: added `export` keyword to `sanitizeMermaidSvg` (one-word change,
  no behavior change).
- `mermaid.smoke.test.ts`: added 5 new tests:
  - `sanitizeMermaidSvg` strips `onerror`, `onclick`, `onmouseover` attributes.
  - `sanitizeMermaidSvg` rewrites `javascript:` and `data:text/html` hrefs/srcs to `#`.
  - `sanitizeMermaidSvg` leaves safe `https://` hrefs and `data:image/png` srcs untouched.
  - `renderMermaidToSvg` fallback: mocked `mermaid.render` rejects → returns code-block
    fallback with original source embedded.
  - `renderMermaidToSvg` fallback: mocked `mermaid.render` resolves with empty svg →
    returns code-block fallback with original source embedded.
  - `vi.mock("mermaid", ...)` used to avoid requiring a real DOM for the async render test.
  - All 3 existing tests preserved unchanged.

## Verification (this exact tree)
- `npx tsc -p tsconfig.json --noEmit`: clean (exit 0)
- `npx eslint mermaid.smoke.test.ts src/markdown/mermaid.ts`: 0 errors
- `node scripts/checkVersion.js`: pass
- `npx vitest run mermaid.smoke.test.ts`: 14 tests passed (3 files)
- `npx vitest run`: **30,552 tests passed** (1,621 files, up from 30,547 baseline + 5 new)
- No Rust/Android source touched.
- No `MarkdownPreview.tsx` change (criterion 4 satisfied).

## Self-review findings
- The `vi.mock("mermaid", ...)` pattern is the idiomatic Vitest approach for module mocking;
  it replaces the module in the registry before the test file's own `import` resolves, so
  `renderMermaidToSvg`'s internal `mermaid.render` call hits the mock.
- The sanitizer's `on\w+=` regex is broad enough to catch any `on*` attribute (onerror,
  onclick, onmouseover, onfocus, etc.) — verified by the 3-attribute test.
- The `javascript:` / `data:text/html` regex handles both quoted and unquoted attribute
  values, matching the existing implementation's intent.
- The fallback path test uses `mockRejectedValueOnce` and `mockResolvedValueOnce` to
  isolate each failure mode without cross-contamination.
- No change to `MarkdownPreview.tsx` or the existing smoke tests — criterion 4 satisfied.

## Status log
- [x] Claimed on main (bde5c0f base), ownership confirmed with margin.
- [x] Export sanitizeMermaidSvg.
- [x] Add sanitizer unit tests (3 tests).
- [x] Add fallback-path test (2 tests).
- [x] Run verification suite (all green, 30,552 tests).
- [x] Self-review (separate findings above).
- [ ] Independent review (new context, local endpoint only) if practical.
- [ ] Finish item + push to main + CI green.
- [ ] Update this handoff with exact landed SHA and CI state.
