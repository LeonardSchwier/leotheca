Task: rm-5ee28000a2f8c8e5 — Print the active note (desktop)
Owner/token: claude-code-cloud-20260921T181726Z-98bdc81f, released (finished)
Scope: src/export/printNote.ts, src/export/printNote.test.ts, src/app/App.tsx; resource note-export-print

Acceptance: a "Print note" command exists in the command palette on desktop (hidden on
Android/Capacitor) when a text note is open. Invoking it while Preview or Split view is
showing the current note prints that note's already-rendered Preview HTML, standalone
(no app chrome), via the OS print dialog (which offers "Save as PDF" on every desktop
platform this app ships for). Invoking it while only Source view is showing tells the
user to switch view rather than silently doing nothing.

Checkpoint: none needed — implemented and landed directly on main in one push (claim
0e requires small, focused scope; this fit in a single push).

Landed: 3dbac7a on main (claim: 949752e; roadmap split: 218b7f7)

Checks: `npx tsc -p tsconfig.json --noEmit` clean; `npm run lint` (eslint) clean;
`npm run check-version` clean; `npx vitest run` 2780/2780 (147 files), up from 2773 on
this branch's base — the 7 new cases are in src/export/printNote.test.ts; `npx vite
build` clean. No Rust or Android source touched by this change, so RUST_CHECKS/
ANDROID_CHECKS are not applicable per skills/verification-suite.md's table.

Review: traced the success path (note open, Preview/Split visible → command reads
`#primary-editor-panes .markdown-preview`'s live innerHTML → `printNoteHtml` builds a
standalone document, writes it into a throwaway hidden iframe, calls `window.print()`
on the iframe's own window so only that content prints, not the app chrome) and the
failure/guard path (Source view only → container not found → `window.alert` guidance,
no crash). Findings from self-review, both fixed before landing: (1) the iframe cleanup
originally relied on a bare `setTimeout`, which could remove the iframe while an
asynchronous webview print flow was still reading it; added an `afterprint` listener as
the primary cleanup path with the timeout kept only as a fallback, and a `cleaned` guard
so both paths firing is harmless. (2) title is HTML-escaped before going into `<title>`
(a note name containing `<`/`&`/etc. could otherwise break the generated document or
inject markup); covered by a dedicated test. No data-loss/privacy risk (this feature
only reads already-rendered, already-sanitized Preview HTML and never touches any file
on disk); no async lifecycle risk beyond the iframe cleanup addressed above; no platform
parity issue introduced since the command is desktop-only, matching the ROADMAP split's
explicit scope for this phase.

CI: run 35638173312 (https://github.com/LeonardSchwier/leotheca/actions/runs/35638173312)
on SHA 3dbac7a — see this handoff's own commit for the final observed conclusion; check
that run directly if this note doesn't show one (this session recorded whatever it last
observed before finishing).

Missing evidence: this is a hidden-iframe `window.print()` call — an OS print dialog
actually opening and rendering correctly on a real desktop (Linux/macOS/Windows) is not
something this cloud sandbox can observe directly; the same class of disclosed gap as
several existing ROADMAP items (e.g. "Local Automation Commands"'s OS URL dispatch).
The document-building and iframe-orchestration logic itself is fully unit tested with
injected test doubles standing in for the real print/OS boundary.

Next action: none required for this phase. Follow-on work is split into its own,
separately claimable ROADMAP items: "Export a note to standalone HTML (desktop)" and
"Print/export a note on Android".
Retry: not applicable (finished, not blocked).
