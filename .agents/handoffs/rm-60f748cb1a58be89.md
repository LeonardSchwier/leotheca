Task: rm-60f748cb1a58be89 — Unscoped native fs commands: siblings of the
  write_text_file fix still accept arbitrary paths
Owner/token: claude-20260922T134632Z-9c6feffb / 325fe828bd896a3aeed50e193e3f1382
  (released on finish)
Scope: touch = src-tauri/src/commands.rs, src-tauri/src/lib.rs,
  src/workspace/tauriBridge.ts, src/workspace/tauriBridgeImpl.ts (plus their
  own *.test.ts siblings, claimed defensively but not needed -- see below);
  resource = workspace-write-path (same resource `rm-dfd60513a2eb352c`
  used, since this is the same contract).

Acceptance:
- `read_text_file`, `read_binary_file`, `read_text_files_batch`,
  `find_markdown_files`, `find_all_files`, `find_all_entries`,
  `workspace_stats`, `create_dir`, `rename_path`, and
  `delete_path_permanent` (`commands.rs`) all now refuse a target that is
  neither inside the currently active workspace nor inside this app's own
  config directory, via the same `check_unscoped_path_allowed` gate
  `write_text_file`/`write_binary_file` already used (renamed from
  `check_unscoped_write_allowed` since it now gates reads and deletes too,
  not just writes -- every call site and test updated to match).
- `rename_path` checks both `from` and `to` independently: either end
  alone being unchecked would let a compromised webview move an arbitrary
  file into, or out of, an allowed location.
- The four whole-workspace walk commands (`find_markdown_files`,
  `find_all_files`, `find_all_entries`, `workspace_stats`) still accept the
  workspace root itself as `path` (their only legitimate call shape, per
  `list_dir`'s own doc comment) -- the gate accepts that because the root
  trivially resolves inside itself; deliberately NOT given a second
  `workspace_root` argument like `list_dir`, which would only re-introduce
  the "trusts a frontend-supplied root" characteristic this claim's own
  disclosed-gaps section below already covers.
- `read_text_files_batch` treats a path outside the allowed roots the same
  as an unreadable file: `None` in its own position, not an error for the
  whole batch, and not distinguishable from "missing" by its result shape
  (new dedicated test, see Checks below).
- `delete_path_permanent` and the pre-existing `delete_workspace_path_permanent`
  now share one `delete_path(target: &Path)` helper for the actual
  file-vs-directory removal branch (small in-scope dedup found while
  touching this code, not a new behavior).
- No regression to legitimate callers: every existing dedicated test for
  these ten commands' own behavior (walk depth limits, symlink-escape
  skipping during a walk, hidden-file skip, size/mtime population, rename/
  delete file-vs-directory branching, batch tolerant-missing-file mapping)
  still passes, now exercising the extracted `_core`/shared functions
  directly instead of the gated `#[tauri::command]` wrapper (same pattern
  `rm-dfd60513a2eb352c`'s own `write_unscoped_file` split already
  established, since these commands' new `tauri::AppHandle`/
  `tauri::State<ActiveWorkspaceRoot>` parameters aren't constructible from
  a plain `#[test]`).
- No frontend changes needed: Tauri injects `AppHandle`/`State` from the
  runtime, not from the JS `invoke()` call's own arguments, so
  `tauriBridgeImpl.ts`'s existing `invoke("read_text_file", { path })`-style
  call sites are already correct as-is. Confirmed by a clean `tsc --noEmit`
  and full frontend test suite with zero frontend files touched.

Checkpoint: none needed; landed directly.

Landed: <fill in candidate SHA after push> on origin/main (branch
  agent/rm-60f748cb1a58be89/325fe828bd89).

Checks (this exact tree, fresh main already up to date -- no conflict to
resolve):
- `cargo fmt --check` (src-tauri): clean
- `cargo clippy --all-targets -- -D warnings` (src-tauri): 0 warnings
- `cargo test` (src-tauri): 84 passed, 0 failed (net +1 vs. pre-change: one
  new dedicated containment test for the batch reader; every other test
  count-neutral, redirected to call `_core`/shared functions instead of
  the now-gated command)
- `cargo check` (src-tauri): clean
- `npx tsc -p tsconfig.json --noEmit`: clean
- `npm run lint` (eslint .): clean
- `npm run check-version`: pass
- `npm test` (vitest run): 151 files, 2847 tests passed (unchanged count --
  no frontend file needed changing)
- `npm run build` (vite build): succeeds (pre-existing >500kB chunk
  warning, unrelated to this change, same as `rm-dfd60513a2eb352c` noted)

Revert-confirm: temporarily replaced `read_text_files_batch_core`'s
`check_unscoped_path_allowed(...).ok().and_then(...)` composition with a
bare `fs::read_to_string(target).ok()` (skipping the check entirely) and
re-ran the new
`read_text_files_batch_maps_a_path_outside_the_allowed_roots_to_none_not_an_error`
test: it failed as expected (the "secret" file's content leaked into the
batch result instead of mapping to `None`). Restored the real
implementation; the test passes again and the full 84-test suite is green.
The underlying `check_unscoped_path_allowed` gate itself already has its
own revert-confirmed test suite from `rm-dfd60513a2eb352c` (accepts
workspace/config-dir targets, rejects outside/symlink-escape/sibling-
prefix); reusing that same function for ten commands means re-deriving
those same revert-confirms per call site would just be testing the same
shared function repeatedly, so this claim adds one new revert-confirmed
test for the one genuinely new behavior (batch tolerant-mapping) and
relies on the existing suite plus code review for the other nine
commands' 2-4 line check-then-operate wiring -- the same accepted split
`rm-dfd60513a2eb352c`'s own handoff already used for `write_text_file`/
`write_binary_file` (no test exercises those full commands either, since
building a real `tauri::AppHandle`/`State` in a unit test is
disproportionate to a straight-line composition).

Review: traced a success and failure path for each of the ten commands.
Success: every real caller found via `grep -rn "invoke(\"" src/` for these
command names passes a path that resolves under the active workspace root
(the walk commands, `list_dir`'s siblings) or the app config dir (none of
these ten currently have a config-dir caller, but the gate still allows
one consistently with `write_text_file`). Failure: `check_unscoped_path_allowed`
rejects before any filesystem access for every one of the ten (verified by
reading each function's body -- the check is always the first statement
after parsing `path`/`from`/`to`, before any `fs::` call). Confirmed
`rename_path` cannot be used to escape containment through only one end
being checked, by requiring both `check_unscoped_path_allowed` calls to
succeed independently before any `fs::rename`. Confirmed the four walk
commands' existing in-walk symlink-escape protection
(`is_symlink_escaping_workspace`) is unchanged and still exercised by their
own untouched `_core` bodies -- this claim only adds a check on the walk's
own starting `path`, which that in-walk protection never covered.

CI: pushed directly per verification-suite.md's "all applicable local
checks and review pass; no known code failure -> push directly" rule; a
later worker should confirm the hosted run for the landed SHA.

Missing evidence: none for the changed behavior itself. Disclosed,
deliberate remaining scope (not required for this item, noted for a future
session, same characteristic `rm-dfd60513a2eb352c`'s own handoff already
disclosed for `write_text_file`/`write_binary_file`): the "active workspace
root" these ten commands check against is still only as trustworthy as the
frontend's own `activeWorkspaceRoot` mirror -- a genuinely
Rust-authoritative workspace root (set only via a real native folder-picker
dialog or independently re-derived from the app's own Rust-written config)
remains a separate, larger, out-of-scope hardening effort. Android's
`FolderAccessPlugin`/Java plugin surface was not audited for the same class
of issue as part of this claim (out of touch scope) and should be checked
under its own claim, per `skills/change-quality-gates.md`'s new
trust-boundary requirement (`rm-b9a528da80814b5f`).

Next action: none required for this item; it is done. A future session
could pursue the Rust-authoritative workspace-root hardening noted above,
or the Android plugin-surface audit, as its own separately-scoped item(s).
Retry: not applicable (finished, not blocked).
