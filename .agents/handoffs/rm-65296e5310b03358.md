Task: rm-65296e5310b03358 -- `rebuildLinkIndex`'s request counter aliases across a workspace switch, letting a stale rebuild overwrite a newer workspace's link index
Owner/token: released (finished)
Scope: src/linking/store.ts, src/linking/store.test.ts; resource link-index-rebuild
Acceptance: A rebuildLinkIndex call still in flight when a workspace switch resets the cache can never have its results applied after that reset, even if its captured request number happens to numerically match the next call's.
Checkpoint: agent/rm-65296e5310b03358/08c69fe42899 at f5c9ee7 (also the landed commit)
Landed: f5c9ee7 on main (fast-forward from claim d7a78a1)
Checks: this exact tree -- `npx tsc --noEmit` clean; `npx vitest run` 2150/2150 passing (114 files, +1 new, no regressions); `npx eslint .` 0 errors/1 pre-existing unrelated warning (FileTree.tsx); `npm run check-version` pass; `npx vite build` pass; from src-tauri (no Rust touched, health check): `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` clean, `cargo test` 62/62 passing, `cargo check` clean.
Review: traced the exact aliasing mechanism -- `resetLinkIndexCache` (fired on every workspace switch via `App.tsx`'s `workspaceTransitions.registerReset`) reset `latestIndexRequest` to 0, so the first `rebuildLinkIndex` call after a reset always got `request = 1`, the same number the session's actual first-ever call got. A stale in-flight call from the just-left workspace that also had `request = 1` would then pass its own `isCurrentRequest()` check after the switch, overwriting `linkIndex.value` (and its persisted on-disk cache) with the wrong workspace's data. Confirmed this isn't hypothetical by finding the sibling fix this codebase already made for the identical problem: `fileOpenAuthority.ts`'s own doc comment explains exactly why `fileOpenGeneration` is never reset (Android's synthetic `/workspace` path makes two different workspace grants otherwise indistinguishable). Fixed the same way, removing the reset rather than adding a second session-token dependency, since nothing else in the file reads the counter's absolute value.
CI: hosted run for f5c9ee7 not yet observed at finish time; a later session should confirm its conclusion and fix any real failure under this same claim.
Missing evidence: hosted CI conclusion for f5c9ee7 not yet observed. No real on-device Android confirmation (this cloud sandbox has no physical Android device); grounded in the real `isCurrentRequest`/`resetLinkIndexCache` implementation and the already-shipped sibling fix in `fileOpenAuthority.ts`, not assumed. The regression test uses controlled promise timing (`vi.waitFor`) rather than a real device race, the same class of gap noted on this codebase's other async-lifecycle tests.
Next action: check hosted CI's conclusion for f5c9ee7; fix any real failure it surfaces.
Retry: available now if a later session finds that run red.
