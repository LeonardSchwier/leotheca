Task: rm-79ee3f1fa7812b5b -- A Desktop workspace folder literally named "workspace" is misnamed "Workspace" instead of using its real name
Owner/token: released (finished)
Scope: src/settings/workspaceProfiles.ts, src/settings/workspaceProfiles.test.ts, src/settings/globalConfig.ts, src/settings/globalConfig.test.ts; resource workspace-profile-naming
Acceptance: A real Desktop folder whose basename happens to be "workspace" gets its real name as the default profile name, not the generic "Workspace" fallback; only the actual Android synthetic root path is special-cased.
Checkpoint: agent/rm-79ee3f1fa7812b5b/7ff585e1b6e2 at 5daf220 (also the landed commit)
Landed: 5daf220 on main (fast-forward from claim 10a65ac)
Checks: this exact tree -- `npx tsc --noEmit` clean; `npx vitest run` 2152/2152 passing (114 files, +2 new, no regressions); `npx eslint .` 0 errors/1 pre-existing unrelated warning (FileTree.tsx); `npm run check-version` pass; `npx vite build` pass; from src-tauri (no Rust touched, health check): `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` clean, `cargo test` 62/62 passing, `cargo check` clean.
Review: both `defaultProfileName` (workspaceProfiles.ts) and the legacy-migration naming (globalConfig.ts) detected the Android synthetic root via a basename check (`basename !== "workspace"`), which conflates "the Android synthetic root path" with "any folder whose last segment happens to be the string workspace" -- a common dev-folder name. Confirmed the same bug existed in both functions (their own doc comments say they mirror each other exactly), and that the actual Android root is always the single literal path `"/workspace"` (`WORKSPACE_ROOT` in `capacitorBridgeImpl.ts`), never a deeper path -- so comparing the *whole* path against that literal fully disambiguates the two cases. Kept the literal duplicated locally in both functions (with a comment explaining why) rather than importing the platform bridge module, respecting workspaceProfiles.ts's own stated "no platform calls" design constraint.
CI: hosted run for 5daf220 not yet observed at finish time; a later session should confirm its conclusion and fix any real failure under this same claim.
Missing evidence: hosted CI conclusion for 5daf220 not yet observed. This is a naming/display defect, not a security or data-integrity one, so severity is low; no real-device confirmation needed since the fix is a pure string comparison verified directly against the real WORKSPACE_ROOT constant's value.
Next action: check hosted CI's conclusion for 5daf220; fix any real failure it surfaces.
Retry: available now if a later session finds that run red.
