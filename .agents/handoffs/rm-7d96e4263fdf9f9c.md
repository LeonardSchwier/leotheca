Task: rm-7d96e4263fdf9f9c -- Desktop `list_dir` has no workspace-containment check, letting a symlink inside the workspace expose arbitrary filesystem content through the file-tree sidebar
Owner/token: ClaudeCode-cloud-scheduled-kindbardeen-20260910T122351Z-10b2f76b / e75922e584a4bd203e4747b913579f6e
Scope: src-tauri/src/commands.rs; src/workspace/tauriBridgeImpl.ts; src/workspace/capacitorBridgeImpl.ts; src/workspace/tauriBridge.ts; src/workspace/fileTreeStore.ts; src/settings/store.ts; src/capture/captureCommit.ts (plus their tests); resource workspace-read-traversal
Acceptance: `list_dir` (the command behind the always-visible file-tree sidebar) rejects listing a target that resolves outside the workspace root (following a symlink), and skips -- without erroring the whole listing -- any child entry that is itself a symlink escaping the workspace, mirroring the already-landed whole-workspace-traversal fix's `is_symlink_escaping_workspace` policy. DONE, revert-confirmed.
Checkpoint: none needed; implemented, verified, and landing directly in one commit.
Landed: e8221f8 on main (fast-forward from claim 750611d), confirmed via git merge-base --is-ancestor after a fresh fetch.
Checks (this exact tree, 2026-09-10, after apt-installing this cloud sandbox's missing libwebkit2gtk-4.1-dev/libgtk-3-dev/librsvg2-dev/libayatana-appindicator3-dev):
  - cargo test (from src-tauri): 60/60 passing (58 pre-existing + 2 new: `list_dir_does_not_surface_a_symlink_escaping_the_workspace`, `list_dir_rejects_a_target_that_is_itself_a_symlink_escaping_the_workspace`).
  - cargo fmt --check: clean.
  - cargo clippy --all-targets -- -D warnings: clean.
  - cargo check: clean.
  - npx tsc --noEmit: clean.
  - npx vitest run: 114 files / 2135 tests passing (unchanged count; only call-site/mock-signature updates, no new TS tests -- the security-relevant logic lives in Rust and is covered there).
  - npx eslint .: 0 errors, 1 pre-existing unrelated warning (FileTree.tsx react-hooks/exhaustive-deps).
  - npm run check-version: pass.
  - npx vite build: pass (pre-existing chunk-size warning, unrelated).
  - Revert-confirmed (Rust): temporarily removed both the `list_dir` top-level containment check and the per-child `is_symlink_escaping_workspace` skip -- both new regression tests failed for the right reason (an escaping symlink was listed; a direct read through an escaping symlink target succeeded instead of erroring); restored and reconfirmed green.
Review: Found during a maintenance review of the just-landed whole-workspace read-traversal symlink-escape fix (`779bd68`/`1c282ff`), which explicitly disclosed this exact follow-up as out of its own scope ("`list_dir` has the same underlying gap but is out of scope here... a distinct, larger follow-up"). Traced the real failure path: a symlink placed inside the workspace pointing outside it (a directory, or a file) appeared in the file-tree sidebar as an ordinary entry (`list_dir` had no containment check at all, unlike its four sibling read-traversal commands), and clicking a file entry routed straight to `read_text_file` (also uncontained) on the symlink's own path, disclosing the external file's real content -- more directly user-triggerable than the already-fixed whole-workspace walks, since this is what drives the always-visible sidebar, not just search/diagnostics/stats. Fixed by giving `list_dir` a second `workspace_root` argument, matching every workspace-scoped mutation's already-established `(workspace_root, ...)` IPC convention (`trash_path`, `create_workspace_dir_new`, etc.): the Rust command now canonicalizes `workspace_root` once, rejects `path` outright if its canonical form doesn't start with that root (reachable only if an earlier listing already surfaced an escaping symlinked directory and the caller expanded it), and skips (not errors) any child entry that is itself an escaping symlink, via the existing `is_symlink_escaping_workspace` helper. Threaded `workspaceRoot` through every frontend call site: `tauriBridgeImpl.ts`/`tauriBridge.ts`'s dispatcher (both now take `(workspaceRoot, path)`), `capacitorBridgeImpl.ts` (signature-only, unused -- a SAF `content://` URI cannot alias outside its granted tree the way a POSIX symlink can, and `isWorkspacePath` already refuses any path this module didn't itself derive from that grant, so Android has no equivalent gap), and every caller in `fileTreeStore.ts` (9 call sites: `loadChildren`, `createNote`, `createCanvasQuick`, `createInkQuick`, `createNoteQuick`, `listTemplates`, `createNoteFromTemplate`, `createFolder`, `renameEntry`; each already had `requireWorkspacePath()`/its own workspace-root parameter available, just called after the `listDir` in most cases -- reordered ahead of it, a behaviorally-neutral move since `requireWorkspacePath()` already threw before any mutation on the exact same missing-workspace condition), `settings/store.ts` (2 sites: `setWorkspacePath`'s `connectIncoming` and `relinkWorkspaceProfile`, both validating a *candidate* root before it becomes the active workspace, so `workspaceRoot === path` there by construction), and `capture/captureCommit.ts` (3 sites, all already had `workspaceRoot`/`root` in scope). Updated every affected test: `FileTree.test.tsx`'s `../settings/store` mock needed a `workspacePath` export it previously lacked (a latent gap this change surfaced, not introduced -- write-path helpers already read it, just never exercised by that test file's expand/select/click tests until `loadChildren` started calling `requireWorkspacePath()` too), and every `listDir` `mockImplementation`/`toHaveBeenCalledWith` across `FileTree.test.tsx`, `fileTreeStore.test.ts`, `settings/store.test.ts`, and `tauriBridge.test.ts` needed its single `path` parameter/assertion updated to the new `(workspaceRoot, path)` shape (a real, if narrow, correctness bug in the fix's first pass: the old single-param mock implementations silently matched against the *new* first argument, `workspaceRoot`, which is constant across every call in these tests, causing every mocked `listDir` call to return the same result regardless of which directory was actually being listed -- caught by the full suite failing, not by the type checker, since the mocks were typed loosely enough to accept the extra argument silently).
CI: not consulted for this exact commit at handoff-write time; will inspect the real hosted CI run on the landed SHA per this repo's own policy (missing CI is not a pass, but full local coverage above permits direct landing without waiting).
Missing evidence: no manual/on-device UI smoke test of the file-tree sidebar itself was performed in this sandbox (no interactive display), the same class of disclosed gap this repo's other UI-only changes record; low risk here since the change is covered by direct Rust filesystem tests against real symlinks (not mocked) plus the existing extensive TypeScript suite exercising every call site's argument wiring.
Next action: none; finished. (Hosted CI run 34477895237 was queued at finish time; check it for the landed SHA if following up.)
Retry: n/a (not blocked, done).
