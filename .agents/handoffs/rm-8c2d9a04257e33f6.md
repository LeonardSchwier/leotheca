# Handoff: Accessibility / screen-reader audit

## Session
- AGENT_ID: hermes-local-20260921T003126Z-6b378149
- Date: 2026-09-21
- Item: rm-8c2d9a04257e33f6
- Branch: agent/rm-8c2d9a04257e33f6/a7ebd48c9d65
- Landed SHA: 29a84d0

## What was done
Audit of the shipped app against WCAG basics (screen-reader labeling, focus
order, keyboard reachability).

### Fixed
Three icon-only `modal-close` buttons were labeled only "x" with no
`aria-label`, making them indistinguishable to screen readers:

1. `src/settings/SettingsPanel.tsx` (settings panel close) → `aria-label="Close settings"`
2. `src/settings/SettingsPanel.tsx` (license viewer close) → `aria-label="Close license viewer"`
3. `src/app/MarkdownHelpDialog.tsx` (help dialog close) → `aria-label="Close Markdown help"`

### Confirmed (no fix needed)
- All other buttons have visible text or existing `aria-label`
- FileTree uses proper roving tabindex (only focused item has `tabindex=0`)
- TabBar has `aria-label` on close buttons and pin indicators
- App toolbar has `aria-label` on all icon buttons
- WorkspaceSwitcher has `aria-label` on forget buttons
- Settings inputs have visible labels or `aria-label`
- Dialogs use `role="dialog"` (WorkspaceSwitcher, ExternalFileView, Sheet)
- `aria-live="assertive"` on settings save errors
- `role="alert"` on save error bar
- `role="status"` on note lock bar

## Verification
- `npx vitest run`: 144 test files, 2716 tests passed
- `npx tsc --noEmit`: clean
- `npx eslint src/settings/SettingsPanel.tsx src/app/MarkdownHelpDialog.tsx`: clean
- `python3 scripts/check_roadmap_format.py`: OK
- CI: Agent policy ✅, Main CI ✅, Release ✅ (all on main @ 29a84d0)

## Claims released
- rm-8c2d9a04257e33f6 → done
