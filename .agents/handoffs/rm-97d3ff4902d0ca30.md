Task: rm-97d3ff4902d0ca30 -- Capture attachment filenames are never sanitized before being used to build a filesystem write path, and the write goes through the uncontained bridge function
Owner/token: released (finished)
Scope: src/capture/captureCommit.ts, src/capture/captureCommit.test.ts, src/capture/pendingCaptures.ts, src/capture/pendingCaptures.test.ts; resource capture-attachment-copy
Acceptance: An attachment's fileName (untrusted on Android, from a share intent's content-provider display name) can never place a copied attachment outside the intended attachments folder, whether via a lexical ".." segment or an embedded "/".
Checkpoint: agent/rm-97d3ff4902d0ca30/c56e2e3ac643 at a036f91 (also the landed commit)
Landed: a036f91 on main (fast-forward from claim 01a20bc)
Checks: this exact tree -- `npx tsc --noEmit` clean; `npx vitest run` 2146/2146 passing (114 files, +2 net new, no regressions); `npx eslint .` 0 errors/1 pre-existing unrelated warning (FileTree.tsx); `npm run check-version` pass; `npx vite build` pass; from src-tauri (no Rust touched, health check): `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` clean, `cargo test` 62/62 passing, `cargo check` clean.
Review: `PendingAttachment.fileName`'s own doc comment claims "(sanitized)" but nothing ever sanitized it; the existing `sanitizeAttachmentFilename` (pendingCaptures.ts) had zero call sites outside its own test. `captureCommit.ts`'s `generateAttachmentFilename` built a destination filename straight from that raw, potentially attacker-controlled name (Android's share intent supplies it from another app's content-provider display name), and the actual disk write went through the plain, uncontained `writeBinaryFile` (an absolute path, no containment check) rather than the workspace-scoped `writeWorkspaceBinaryFile`. Traced both platforms' real failure mode: desktop's `fs::write`/`fs::create_dir_all` genuinely resolves ".." at the OS level (a real escape); Android's SAF document-creation API has no such operator (a literal, merely-ugly ".."-named subfolder, not a real escape) -- still worth closing at the root rather than assuming SAF's happenstance safety is permanent. Fixed at both the ingestion point (sanitize in `addPendingCapture`, the one choke point for every attachment source) and the write boundary (switch to `writeWorkspaceBinaryFile`, whose Rust-side `resolve_within_workspace` rejects any ".." component), so neither fix alone has to be perfect for the other to still hold.
CI: hosted run for a036f91 not yet observed at finish time; a later session should confirm its conclusion and fix any real failure under this same claim.
Missing evidence: hosted CI conclusion for a036f91 not yet observed. No real on-device Android share-intent test (this cloud sandbox has no physical Android device); the fix is grounded in a direct reading of `androidShareBridge.ts`'s real untrusted data path and the SAF plugin's actual document-creation API, not assumed.
Next action: check hosted CI's conclusion for a036f91; fix any real failure it surfaces.
Retry: available now if a later session finds that run red.
