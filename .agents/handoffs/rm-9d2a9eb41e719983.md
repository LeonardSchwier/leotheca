Task: rm-9d2a9eb41e719983 — `saveCoordinator.flush()` bumps the revision before writing, creating a phantom unsaved-work state after every flush
Owner/token: hermes-local-20260921T150604Z-3852b515 / 270d4ea60b3b48b185f3fa0c8b0e90ef
Scope: src/workspace/saveCoordinator.ts (flush function), src/workspace/saveCoordinator.test.ts (regression tests); resource: save-coordinator
Acceptance:
  1. After `flush()` on a note with no pending edits, `hasUnsavedWork(session)` returns false. — PASS
  2. After `flush()` on a note with a pending (debounced, not-yet-fired) edit, the edit is written and `hasUnsavedWork(session)` returns false. — PASS
  3. `flush()` correctly rejects when the write fails (`getError` returns the message, `hasUnsavedWork` stays true). — PASS
  4. `flush()` on a session that has never had `change()` called is a no-op (no write, no spurious dirty state). — PASS
  5. Existing saveCoordinator tests continue to pass unchanged. — PASS (22/22 in saveCoordinator.test.ts)
  6. `retry()` behavior is unchanged (it intentionally bumps revision as a new save attempt). — PASS
Regression evidence: Reverting the fix causes 1 of 6 new tests to fail (the no-op test for never-edited notes) and the full suite to pass 21/22. The in-flight race test passes in both cases because the reschedule mechanism masks the phantom bump there, but the semantic fix is still correct: flush should write at the current revision, not a fabricated one.
Checkpoint: agent/rm-9d2a9eb41e719983/270d4ea60b3b (work branch)
Landed: not landed yet (pending push)
Checks (all run in this session on this exact tree):
  npx tsc -p tsconfig.json --noEmit → exit 0
  npm run lint → exit 0
  npm run check-version → exit 0 (0.1.0 consistent)
  npm test → 146 files, 2771 tests, all passed
  npm run build → success (4.66s)
  git diff --check → clean
  Targeted: npx vitest run src/workspace/saveCoordinator.test.ts → 22/22 passed
  Regression: reverted fix → 21/22 (1 failed: never-edited no-op test), restored → 22/22
Review (self-review, not independent):
  - Scope: minimal — one function modified, one guard clause added, no other functions touched.
  - Correctness: writeRevision checks `entry.revision === revision` before setting savedRevision. flush now passes the current entry.revision (not a bumped value), so the check passes and savedRevision is set correctly. The finally-block reschedule guard (`entry.revision > revision`) correctly handles newer edits arriving mid-write.
  - Edge case: revision === 0 (never edited) returns early, avoiding a spurious write.
  - retry() unchanged: still bumps revision as an intentional new save attempt.
  - No data-loss risk: the fix only changes which revision number is passed to writeRevision; the content written is always entry.latestContent (unchanged).
  - Test quality: 6 new tests covering clean flush, pending-edit flush, error path, no-op, in-flight race, and retry invariance.
CI: not yet pushed; will check after push
Missing evidence: hosted CI for this exact candidate SHA (will verify after push)
Next action: commit, push to work branch, check CI, then merge to main
Retry: not needed
