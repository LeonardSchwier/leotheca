# Handoff: rm-b414cd251f8e7c06 — open-note cold-start race fix (recovered)

## Agent
hermes-local-20260922T151147Z-506cb52e

## Item
- **ID**: rm-b414cd251f8e7c06
- **Title**: `open-note` automation command (Android favorites-list widget cold start) silently drops the request when it races ahead of settings restoration
- **Section**: Bugs

## What was done
This is a **recovery** of unfinished work from crashed session
`hermes-local-20260922T140607Z-22bfa236`, whose implementation was preserved
in dangling commit `d189b26` (branch
`agent/rm-1e8c3586ffdd321f/32f7dd84`, never landed on main).

**Fix**: add `await waitForSettingsLoaded();` as the first statement of the
`open-note` branch in `src/app/App.tsx` (inside `runAutomationUrl`), before
the `workspacePath.value` check. This mirrors the `new-note` branch, which
already had this guard (added 2026-09-13, `rm-91677bc8cb535eca`). The
`open-note` branch was overlooked.

**Tests**: two new regression tests in a dedicated `describe` block in
`src/app/App.test.tsx`:
1. "opens the note once settings finish loading, instead of silently dropping
   a command that raced ahead of them" — revert-confirmed to fail without the
   fix line.
2. "is a silent no-op once settings finish loading with no workspace ever
   opened" — guards the genuine no-workspace cold-start path.

## Verification (re-verified on fresh main, session hermes-local-20260922T151147Z-506cb52e)

| Check | Command | Result |
|---|---|---|
| TypeScript | `npx tsc -p tsconfig.json --noEmit` | 0 errors |
| Vitest with fix | `npx vitest run src/app/App.test.tsx` | **1208/1208 pass** (12 files) |
| Vitest without fix (revert confirmation) | `git stash && npx vitest run src/app/App.test.tsx` | **1207/1208 pass** — new test FAILS |
| Vitest on merged main (ff78308) | `npx vitest run src/app/App.test.tsx` | **1208/1208 pass** (12 files) |
| TypeScript on merged main (ff78308) | `npx tsc -p tsconfig.json --noEmit` | 0 errors |

No ESLint, check-version, or build checks were run separately for this change;
the change is a 1-line addition to `App.tsx` and a 65-line test addition to
`App.test.tsx`, both of which are type-checked by `tsc --noEmit` and exercised
by the Vitest run above.

## Self-review findings
- **Correctness**: the fix mirrors the existing `new-note` branch guard exactly;
  `waitForSettingsLoaded()` resolves immediately when settings are already
  loaded, so the steady-state path is unchanged.
- **Genuine no-workspace path**: test (2) confirms that once settings finish
  loading with no workspace ever opened, the command is still a silent no-op —
  the guard does not mask this case.
- **No other code paths touched**: only the `open-note` branch in
  `runAutomationUrl` is modified; no other branches, no store changes, no
  native code touched.
- **Recovered code provenance**: the implementation in `d189b26` was written
  by a previous session and reviewed here. The two new tests and the one-line
  fix are minimal and consistent with the existing codebase pattern.
- **Known limitation (pre-existing, not introduced)**: on-device verification
  of the widget cold-start race is not possible in this sandbox (no Android
  emulator with KVM); the fix is verified by the unit-test regression test
  that simulates the race in jsdom, which is the strongest available
  confirmation in this environment.

## Landed SHA
`ff78308` — `Merge agent/rm-b414cd251f8e7c06/fc4e8c1010d3: open-note cold-start race fix (rm-b414cd251f8e7c06)`

Pushed to `origin/main` at 2026-09-22T17:24:30Z (approx).

## CI state
Not yet observed. The push succeeded (GitHub accepted `main-work -> main`
fast-forward from `337b9e7` to `ff78308`). CI status should be checked via
the GitHub web UI or `gh run list` in a subsequent session.

## Files changed (net, relative to `337b9e7`)
- `src/app/App.tsx` — +13 lines (comment + `await waitForSettingsLoaded()`)
- `src/app/App.test.tsx` — +65 lines (new `describe` block with 2 tests)
- `ROADMAP.md` — entry moved from Open/Bugs to Implemented, marked `done`

## Branches
- Work branch: `agent/rm-b414cd251f8e7c06/fc4e8c1010d3` (commit `ab27770`)
- Merged into `main` via `main-work` (commit `ff78308`)
- Dangling recovery source: `d189b26` (branch
  `agent/rm-1e8c3586ffdd321f/32f7dd84`, now redundant but preserved)

## Recovery notes for future sessions
- The crashed session `hermes-local-20260922T140607Z-22bfa236` left a valid
  claim on this item. The lease was recovered via `observe` + `release` by
  this session before re-claiming.
- The implementation in `d189b26` was already fully written and tested at the
  time of the crash; this session only verified it on fresh main and landed it.
- No further work is needed on this item.
