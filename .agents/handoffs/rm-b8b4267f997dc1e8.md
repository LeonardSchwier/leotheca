Task: rm-b8b4267f997dc1e8 -- `renameExecutor.ts`'s Apply and rollback steps corrupt note content whenever a rename's wikilink edits share a file or the renamed basename changes length
Owner/token: Claude-Code-cloud-scheduled-kindbardeen-20260912T231601Z-54a9566c | 7be24a12bf37e0f948c2ebe79f2a11dd
Scope: src/refactor/renameExecutor.ts, src/refactor/renameExecutor.test.ts; resource rename-migration
Acceptance: Forward-apply groups a note's edits and applies them in one read-modify-write pass (right-to-left by offset), so a note linked twice from the renamed note is never corrupted by a stale offset. Rollback restores each edit by the actual current length of the text it wrote (not the pre-edit end offset), and only overwrites a span whose content still matches what was actually written, so it never corrupts a note whose own edit never landed during a partial multi-file forward-apply failure.
Checkpoint: agent/rm-b8b4267f997dc1e8/7be24a12bf37 (also the landed branch before merge)
Landed: pending this control transaction's push to main
Checks: this exact tree -- `npx tsc -p tsconfig.json --noEmit` clean; `npx vitest run` 2218/2218 passing (117 files, +3 new: two-edits-one-file forward apply, length-changing rollback, partial-multi-file rollback safety; no regressions); `npx eslint .` 0 errors; `npm run check-version` pass; `npx vite build` pass; from src-tauri (no Rust touched, health check after bootstrapping this cloud sandbox's WebKitGTK/GTK3/appindicator libraries): `cargo fmt --all -- --check` clean, `cargo clippy --all-targets -- -D warnings` clean, `cargo test` 64/64 passing, `cargo check` clean.
Review: `executeRenameOperation` (F03 Phase 2b remainder, `rm-eab533a0187dc044`, marked done but never wired into any UI -- confirmed zero callers outside this module and its own test file) had two real bugs, both masked by the existing tests' `mockResolvedValue` `readNote` stubs (fixed content regardless of prior writes): (1) forward-apply read+wrote once per edit instead of once per file, so a second wikilink edit in the same note used an offset stale after the first edit changed the text's length; (2) rollback restored `oldText` at the pre-edit `[from, to)` span read back from the *already-edited* file, which is wrong the instant `newText.length !== oldText.length` (the normal case for a real rename). Fixed both with a shared `applyRangeReplacementsGroupedByPath` helper: groups replacements by path, applies each path's edits right-to-left in one in-memory pass, then writes once. Rollback additionally passes `verify: wikilink.newText` so a replacement is applied only when that exact text is still present at `from`; this closes a third bug my own first fix draft introduced (naively using `newText.length` unconditionally for rollback would have corrupted a note whose own forward edit never landed during a partial multi-file failure -- caught and fixed before landing, with a dedicated regression test using a call-counting mock so the corrupted write, if it happened, would actually be observed rather than swallowed by an always-throwing mock). All three fixes are individually revert-confirmed: each regression test fails for the intended reason with only that specific fix backed out, and passes once restored, with the other tests unaffected throughout.
CI: not yet observed for the landed SHA; a later session/check-in should confirm hosted CI's conclusion (if any -- this app pushes straight to main) and fix any real failure under this same claim's understanding.
Missing evidence: no manual/on-device UI smoke test applies -- this code path has no UI wiring yet (disclosed in the original F03 Phase 2b remainder entry: "keeps apply disabled until all tests pass per spec Phase 2 instruction"), so there is nothing to click through; this is a correctness fix to dormant code, verified via targeted unit tests against the real functions.
Next action: none required; item is complete. A future session wiring an Apply UI on top of `executeRenameOperation` inherits a correct implementation instead of the corruption bugs described above.
Retry: not applicable (finished).
