Task: rm-b8c509ec47419760 -- Maintenance review: a legacy v2 editorLayout without viewMode decodes with viewMode: undefined, violating EditorLayoutState's required field
Owner/token: released (finished)
Scope: src/settings/workspaceSettings.ts, src/settings/workspaceSettings.test.ts; resource editor-layout-persistence
Acceptance: decodeWorkspaceSettings never returns an EditorLayoutState whose primary/secondary viewMode is undefined, for a legacy v2 file that predates that field.
Checkpoint: agent/rm-b8c509ec47419760/d897453ef604 at c93c65f
Landed: c93c65f on main (fast-forward from claim cdec75e)
Checks: tsc --noEmit clean; vitest 2139/2139 passing (114 files, +2 new); eslint 0 errors/1 pre-existing unrelated warning (FileTree.tsx); check-version pass; vite build pass; cargo fmt/clippy -D warnings/test(60)/check all green from src-tauri (no Rust touched, health check, after bootstrapping WebKitGTK/GTK3/appindicator).
Review: isValidEditorLayoutState deliberately accepts a group with no viewMode (a real pre-viewMode v2 file); decodeWorkspaceSettings assigned it straight through, leaving viewMode undefined at runtime despite EditorLayoutState's non-optional type. restorePrimaryEditorLayout then copied that undefined onto the live layout on every workspace open/switch. No current UI reads primary.viewMode (confirmed by grep), so no visible effect today, but the same class of defect this repo already shipped once (rm-673e3fcbd1b2220d main regression). Fixed with withDefaultedViewModes at the one decode choke point, defaulting to the same values createPrimaryEditorLayout/createSplitLayout already use ("source"/"preview").
CI: hosted run 34542459246 (push to main, SHA c93c65f) was in_progress at finish time; not yet confirmed green.
Missing evidence: hosted CI conclusion for c93c65f not yet observed.
Next action: check run 34542459246's conclusion for SHA c93c65f; fix any real failure under this same understanding of the change (frontend-only change, so only the frontend job is expected to exercise it).
Retry: available now if a later session finds that run red.
