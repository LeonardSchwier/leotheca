Task: rm-b9a528da80814b5f — Maintenance review: proactive trust-boundary checks for
  path/URI-accepting native commands
Owner/token: claude-20260922T133437Z-8beff2d1 / e399a8c633b4d31b0afb330490f901cc
  (released on finish)
Scope: touch = CONSTITUTION.md, skills/change-quality-gates.md,
  skills/maintenance-review.md; resource = trust-boundary-quality-gates.

Trigger: while reporting rm-dfd60513a2eb352c's landed `write_text_file`/
  `write_binary_file` containment fix, re-read `src-tauri/src/commands.rs`'s
  full `#[tauri::command]` list and found that `read_text_file`,
  `read_binary_file`, `read_text_files_batch`, `find_markdown_files`,
  `find_all_files`, `find_all_entries`, `workspace_stats`, `create_dir`,
  `rename_path`, and `delete_path_permanent` still call the filesystem
  directly on a caller-supplied path with zero containment -- the exact same
  shape the write commands had before that fix. Existing process
  (`skills/change-quality-gates.md`, `skills/maintenance-review.md`) only
  asked a reviewer to look for "unvalidated IPC" inside whatever bounded
  scope they happened to pick; nothing made a fix for one unscoped command a
  trigger to check its siblings in the same file. This item closes that
  process gap; it does not itself fix the sibling commands (seeded
  separately as `rm-<id>` "Unscoped native fs commands: siblings of the
  write_text_file fix still accept arbitrary paths", an Open Bug under
  `### Bugs`, unclaimed).

Acceptance:
- `skills/change-quality-gates.md` gets a new required check (item 5) and a
  new review question: any path/URI/file-handle argument crossing the
  webview/native boundary must be checked against an explicit allow-list,
  regardless of today's callers, and a command's siblings with the same
  argument shape must be re-checked in the same change.
- `skills/maintenance-review.md`'s "Review for observable defects" section
  now tells a filesystem/IPC-commands reviewer to enumerate every exported
  command/plugin method with a similarly-shaped argument in the same
  file/module, not just the one that prompted the review.
- `CONSTITUTION.md`'s Quality and verification section gets one pointer
  sentence stating the same untrusted-by-default rule, referencing the skill
  for detail, consistent with how the rest of the file stays terse and
  delegates specifics to skills.
- No contradictions introduced: cross-referenced file/skill names
  (`skills/change-quality-gates.md`, `rm-dfd60513a2eb352c`) verified to
  actually exist/match.

Checkpoint: none needed; landed directly.

Landed: <fill in candidate SHA after push> on origin/main (branch
  agent/rm-b9a528da80814b5f/e399a8c633b4).

Checks (docs-only change per skills/verification-suite.md's "Markdown
instructions/docs only" row -- no code, lease helper, or policy workflow
touched, so POLICY_TEST does not apply):
- `git diff --check`: clean, no whitespace/conflict-marker issues.
- Read the full diff; confirmed both referenced identifiers
  (`skills/change-quality-gates.md`, `rm-dfd60513a2eb352c`) are real and
  correctly named, and the new prose does not contradict either skill file's
  existing content or `CONSTITUTION.md`'s surrounding paragraphs.
- `python3 scripts/check_roadmap_format.py --root . --base origin/main`:
  roadmap format OK for both new seeded entries.

Review: self-review only (no independent agent available this session).
Traced that the new required check in `change-quality-gates.md` is
consistent with its own existing numbered-list style and doesn't duplicate
check 1-4; the `maintenance-review.md` addition sits inside the existing
bullet it extends rather than as a disconnected new rule, so a reviewer
following that skill's normal flow actually encounters it. Confirmed the
skill changes are advisory/process only -- no change to `scripts/
agent_ledger.py`, `scripts/check_roadmap_format.py`, or any CI workflow, so
this cannot itself break another agent's claim/finish flow.

CI: none applicable (docs-only commit; main CI path-filters out changes that
  touch only ROADMAP.md/handoffs, and this change touches no application
  code, so no hosted run is expected or required for this candidate).

Missing evidence: none for the stated scope. The sibling unscoped commands
this review surfaced are deliberately NOT fixed under this item -- that is
a separate, larger, code-level claim (seeded as its own Open Bug) with its
own containment logic, tests, and RUST_CHECKS/FRONTEND_CHECKS verification;
bundling it here would have meant either skipping that verification or
inflating this docs-only item's scope past what it claimed.

Next action: a future session claims the seeded "Unscoped native fs
commands" Open Bug and implements the actual containment fix for
read_text_file/read_binary_file/read_text_files_batch/find_markdown_files/
find_all_files/find_all_entries/workspace_stats/create_dir/rename_path/
delete_path_permanent, following the new required check/review question
this item just added.
Retry: not applicable (finished, not blocked).
