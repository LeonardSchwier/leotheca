Task: rm-c5b85829c81dd890 — RTL Phase 1: automatic per-note text direction in the Markdown editor and preview
Owner/token: released (finished)
Scope: src/editor/textDirection.ts, src/editor/MarkdownEditor.tsx, src/editor/MarkdownPreview.tsx (plus tests, documentation/ARCHITECTURE.md); resource editor-text-direction
Acceptance: a note whose content is predominantly right-to-left (Hebrew, Arabic, ...) renders and edits with correct per-line/per-block direction in both the CodeMirror source editor and the Markdown preview, detected automatically via the Unicode Bidi Algorithm's "first strong character" rule, with no workspace-chrome change required
Checkpoint: none needed, landed directly
Landed: 4e5f290 on main (fast-forward from 22aaa44)
Checks: tsc --noEmit clean; vitest 2133/2133 (+31 new, 8 revert-confirmed via git stash of the wiring edits); eslint 0 errors/1 pre-existing unrelated warning; npm run check-version pass; vite build pass; cargo fmt --check/clippy --all-targets -D warnings/test(57)/check all green from src-tauri (no Rust touched, health check, after apt-installing libwebkit2gtk-4.1-dev/libgtk-3-dev/librsvg2-dev/libayatana-appindicator3-dev)
Review: traced both real paths — a Hebrew/Arabic line in the editor (Decoration.line + perLineTextDirection facet, verified against the live CodeMirror DOM in tests) and a Hebrew/Arabic block in the preview (dir="auto" survives DOMPurify.sanitize, verified against real rendered output); found and fixed one review-time defect before landing: a naive `\bdir=` word-boundary check in addAutoTextDirection's "already has a dir attribute" guard would have false-matched an unrelated `data-dir=` attribute (hyphen-to-letter is itself a word boundary), tightened to require a preceding whitespace and locked in with a regression test
CI: https://github.com/LeonardSchwier/leotheca/actions/runs/34389322748 — in_progress as of 2026-09-09T18:30Z; not yet confirmed green
Missing evidence: hosted CI conclusion for the landed SHA not yet observed (still running); no real RTL-script-locale device/desktop manual smoke test performed (this cloud sandbox has no interactive display), same class of disclosed gap this repo's other UI-only changes record — low risk here since the change is plain CSS/DOM-attribute application covered by direct jsdom-rendered assertions against the real CodeMirror view and real DOMPurify-sanitized output, not a platform/native boundary
Next action: re-check https://github.com/LeonardSchwier/leotheca/actions/runs/34389322748 for the frontend/backend/android/appimage-smoke/validation jobs; if a real failure appears, diagnose and fix under this same understanding of the change (see skills/ci-failure-triage.md)
Retry: available now — CI check only, no external blocker
