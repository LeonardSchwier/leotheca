Task: rm-cf44f583b5e7227c — UX-01: Visual system and adaptive UX refresh
Owner/token: released
Scope: Menu primitive slice; `src/ui/Menu*`, primitive styles, and Document Header overflow migration.
Acceptance: The shared Menu owns an accessible disclosure trigger, viewport-fitted popup, initial focus, Arrow/Home/End navigation, Enter/Space activation, disabled items, Escape/outside dismissal, and focus restoration. Document Header uses it without changing its four actions or callback behavior. Focused tests cover keyboard, pointer, disabled, collision, and cleanup paths.
Checkpoint: none
Landed: a0fa46baca627389f0d29b59af3be732f12c05ab on main
Checks: `npm ci` passed. On the final integrated tree: focused Menu/DocumentHeader tests 42/42; `npx tsc -p tsconfig.json --noEmit` clean; `npm run lint` clean; `npm run check-version` passed; `npm test` passed 135 files/2609 tests; `npm run build` passed (523 modules, only the pre-existing chunk/dynamic-import warnings); `git diff --check` clean.
Review: Pointer and keyboard open/select/dismiss paths traced. Native buttons preserve Enter/Space activation; disabled items cannot activate and expose a reason; Escape and selection restore trigger focus before a callback can open its next dialog; outside pointer/blur/Tab dismiss without stealing the user's new focus. Positioning clamps both viewport corners and flips above when bottom space is insufficient. No note, persistence, native bridge, or untrusted HTML path changed.
CI: exact-SHA CI run 35357814115 in progress; Release run 35357814937 queued at release time. No failure observed.
Missing evidence: physical Desktop/Android visual and touch verification is unavailable in this environment.
Next action: confirm CI run 35357814115 and continue UX-01 with the next bounded Dialog/status-primitives slice.
Retry: available now
