Task: rm-cf44f583b5e7227c — UX-01: Visual system and adaptive UX refresh
Owner/token: Codex-GPT5-20260918T142806Z-025f1e3f / 3e0bccfdb6d791e0562da595dd594c4e
Scope: Dialog primitive slice; `src/ui/Dialog*`, primitive styles, and ConfirmDialogHost migration.
Acceptance: The shared Dialog provides labeled title/optional description, configurable Escape and backdrop dismissal, deliberate initial focus, Tab/Shift+Tab focus trapping, focus restoration, viewport-safe scrollable content, stable header/action rows, and modal semantics. ConfirmDialogHost delegates those behaviors without changing confirm/cancel promise results or safe initial focus. Focused tests cover focus, dismissal, trapping, non-dismissible modes, and cleanup.
Checkpoint: none
Landed: Menu slice a0fa46baca627389f0d29b59af3be732f12c05ab; Dialog slice pending publication.
Checks: On the Dialog slice's final integrated tree: focused Dialog/ConfirmDialogHost/App tests 122/122; `npx tsc -p tsconfig.json --noEmit` clean; `npm run lint` clean; `npm run check-version` passed; `npm test` passed 137 files/2632 tests; `npm run build` passed (525 modules, only the pre-existing chunk/dynamic-import warnings); `git diff --check` clean.
Review: Escape reaches only the topmost dialog and is contained for non-dismissible work; backdrop dismissal requires a direct backdrop press; confirm, Cancel, Escape, and backdrop settlement preserve the existing boolean-result contract. Initial focus is deliberate, both focus-trap directions wrap, out-of-modal Tab is recaptured, and opener focus/body overflow restore on unmount. The surface keeps its title and actions fixed around scrollable content, uses safe-area viewport padding, and retains `.modal` only as a reset compatibility hook for existing shell tests. No note, persistence, native bridge, or untrusted HTML path changed.
CI: Menu exact-SHA CI run 35357814115 was in progress and Release run 35357814937 queued when that slice released; Dialog pending.
Missing evidence: physical Desktop/Android visual and touch verification is unavailable in this environment.
Next action: publish the exact verified tree, inspect exact-SHA CI, release this claim, then continue UX-01 with the next bounded status-primitives slice.
Retry: available now
