Task: rm-cf44f583b5e7227c — UX-01: Visual system and adaptive UX refresh
Owner/token: released (formerly Codex-root-20260918T202227Z-91d326ca / 2b23e17b797d172049897f440d1feefe; no implementation was made on this token)
Scope: Document Header workspace-relative path breadcrumb. Touches `src/app/App.tsx`, `src/app/App.css`, `src/app/layout/DocumentHeader.tsx`, and focused component tests; resources `document-header` and `workspace-path-navigation`.
Acceptance: At Medium+ an active text-note header displays parent folders from the workspace-relative path without exposing an absolute filesystem path in its visible content; a root-level note has no empty breadcrumb; the complete native path remains available in the title attribute; title/action truncation behavior and save state are unchanged.
Checkpoint: `agent/rm-cf44f583b5e7227c/f7715f461e7a` at 846f38f859c61cabc4a801413f3309222acae6ff.
Landed: Not landed.
Checks: Lockfile `npm ci` completed (515 packages). `npx vitest run src/app/layout/DocumentHeader.test.tsx --reporter=dot` passed (1 file, 27 tests) and `npx vitest run src/app/App.test.tsx --reporter=dot` passed (1 file, 100 tests; existing mock-warning output from the F05 Android-share handler only). `npx tsc -p tsconfig.json --noEmit`, `npm run lint`, `npm run check-version`, and `npm run build` all passed. The complete `npm test` run exceeded this runner's 30-second command cap twice before reporting a final exit status, so it is not claimed green. `git diff --check` passed.
Review: The visible breadcrumb derives only from the existing workspace-relative path helper and drops the leaf note name, so it neither introduces a second path parser nor reveals an absolute path. The root-path fallback is the current note name, which deliberately prevents a null workspace root from generating a bogus path. The full path tooltip and current save/action structures remain intact. No persistence, native bridge, filesystem mutation, or remote-asset path changed.
CI: No candidate CI inspected.
Missing evidence: complete Vitest-suite exit status is unavailable because the runner terminates commands after 30 seconds; physical Desktop/Android visual verification is unavailable in this environment.
Next action: reclaim with `src/app/App.css` included, then recover checkpoint `846f38f859c61cabc4a801413f3309222acae6ff`, integrate current main, and run the complete frontend suite to a final exit status.
Retry: available now.
