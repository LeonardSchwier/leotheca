Task: rm-d13396323497553d — Export a note to standalone HTML (desktop)
Owner/token: claude-code-cloud-20260921T181726Z-98bdc81f, released (finished)
Scope: src/export/exportNoteHtml.ts, src/export/exportNoteHtml.test.ts,
src/workspace/tauriBridgeImpl.ts, src/app/App.tsx; also (new, not in the original
touch declaration, but a directly-related new test file for an already-claimed
implementation file with zero collision risk) src/workspace/tauriBridgeImpl.test.ts;
resource note-export-print (same resource as the just-finished Phase 1, sequential
not concurrent).

Acceptance: an "Export note to HTML…" command exists in the command palette on
desktop (same !Capacitor.isNativePlatform() gate as "Print note") when a text note
is open. Invoking it while Preview or Split view shows the current note opens a
native Save dialog defaulting to "<note name>.html"; on confirmation, writes a
standalone HTML file with the note's rendered Preview content, with every local
attachment image inlined as a data: URI (not the app's internal asset:// URL, which
only resolves inside a running Leotheca instance) so the file is genuinely openable
standalone. Cancelling the dialog does nothing (no file written, no error). Guard:
Source-view-only shows the same "switch to Preview or Split view" alert Print uses.

Checkpoint: none needed — implemented and landed directly on main in one push.

Landed: 1761e00 on main (claim: 5290bf0)

Checks: `npx tsc -p tsconfig.json --noEmit` clean; `npm run lint` clean; `npm run
check-version` clean; `npx vitest run` 2789/2789 (149 files), up from 2780 on this
branch's base — the 9 new cases split across src/export/exportNoteHtml.test.ts (image
inlining behavior) and src/workspace/tauriBridgeImpl.test.ts (the new
pickHtmlExportPath's real save() call shape); `npx vite build` clean. No Rust or
Android source touched.

Review: traced the success path (Preview/Split visible → Save dialog → image
inlining via fetch()+data: URI → writeTextFile to the user-chosen path) and every
failure/guard path: Source-view-only (alert, no dialog opened), dialog cancelled
(silent no-op, not treated as an error), a single image failing to fetch (left with
its original src, export still completes — tested), pickHtmlExportPath/writeTextFile
throwing (caught, surfaced via window.alert, matching this codebase's existing
error-alert convention elsewhere in App.tsx). No data-loss/privacy risk: the write
target is a path the user explicitly chose via a native OS dialog (the same
unscoped-absolute-path write() already used for the app's own settings file), and the
exported content is exactly the note's own already-rendered, already-sanitized
Preview output plus its own images, nothing else. A nice byproduct of reusing the
live DOM rather than a fresh render: an already-resolved cross-note embed's own
content (and any local images inside it) is captured and inlined the same as the
host note's own images, with no special-case code needed for it.

CI: run 35639046385 (https://github.com/LeonardSchwier/leotheca/actions/runs/35639046385)
on SHA 1761e00 — was queued at finish time; check it directly on `main` if this note
doesn't show its conclusion. Phase 1's own equivalent run (35638173312, SHA 3dbac7a)
completed with conclusion "success" before this item was finished, confirming the
same CI path is healthy for this feature area.

Missing evidence: a real native Save dialog actually opening and a real OS file
write actually succeeding on a real desktop is not something this cloud sandbox can
observe directly — the same class of disclosed gap as Phase 1's own OS-print-dialog
gap and several other existing ROADMAP items. pickHtmlExportPath's own call shape
(the exact save() arguments) and inlineLocalImages' image-fetching/fallback behavior
are both fully unit tested against injected test doubles standing in for that
OS/native boundary.

Next action: none required for this phase. Remaining scope: "Print/export a note on
Android", left open as its own separately claimable ROADMAP item.
Retry: not applicable (finished, not blocked).
