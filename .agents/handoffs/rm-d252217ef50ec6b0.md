Task: rm-d252217ef50ec6b0 -- Maintenance review: `appendToInboxNote` silently discards a capture's existing inbox-note content when the append write fails but a later write attempt succeeds
Owner/token: ClaudeCode-cloud-scheduled-20260913T231126Z-c2f66dc4, released (finished)
Scope: src/capture/captureCommit.ts, src/capture/captureCommit.test.ts; resource capture-commit
Acceptance: only a failed readTextFile selects the create-fresh-file path; a failed append writeTextFile propagates as a rejection instead of a second, content-dropping write; regression test proves a failed-then-later-successful append no longer loses existingContent.
Checkpoint: none needed (landed directly)
Landed: 373d68e0ea2354468fac797019d1309e84517169 on main
Checks: tsc --noEmit clean; npx vitest run 118 files/2234 tests passing (+1 new); eslint 0 errors; npm run check-version pass; npx vite build pass; cargo fmt --check/cargo test 64/64/cargo clippy --all-targets -- -D warnings/cargo check all pass (health check, no Rust source touched)
Review: traced appendToInboxNote's three real callers (CaptureSheet.tsx, captureProcessor.ts, PendingCapturesPanel.tsx); all three already wrap the call in try/catch and handle a rejection by surfacing an error or keeping the capture pending, so propagating the write failure is a strict improvement with no caller-side regression.
CI: run 34789616607 (CI) and 34789616833 (Release) were in_progress/queued for 373d68e at finish time
Missing evidence: hosted CI conclusion for 373d68e not yet confirmed
Next action: check runs 34789616607 / 34789616833 for 373d68e; fix any real failure under a fresh claim of the same scope if one appears
Retry: available now (this is a completed item; only relevant if hosted CI later reveals a real failure)
