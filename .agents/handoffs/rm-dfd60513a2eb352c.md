Task: rm-dfd60513a2eb352c — Unscoped `write_text_file` commands accept arbitrary paths
Owner/token: claude-20260922T122235Z-9d54ddd1 / b8d60ea2bbb05696030c5347d69ae166 (released on finish)
Scope: touch = src-tauri/src/commands.rs, src-tauri/src/lib.rs, src/app/App.tsx,
  src/workspace/tauriBridge.ts, src/workspace/tauriBridgeImpl.ts (plus their own
  *.test.ts siblings); resources = workspace-write-path, html-export. Reclaimed
  a stale legacy claim (owner hermes-local-20260922T101719Z-1e30a773, lease
  expired 2026-09-22T12:00:03Z, no branch was ever pushed for it).

Acceptance:
- `write_text_file`/`write_binary_file` (`commands.rs`) refuse any target that
  is neither inside the currently active workspace nor inside this app's own
  config directory, closing the "any absolute path, no containment at all"
  gap a compromised webview could reach directly over Tauri IPC, bypassing
  every frontend wrapper.
- The "Export note to HTML..." desktop flow (`App.tsx`) no longer round-trips
  a webview-suppliable path through the unscoped writer at all: a new atomic
  `export_text_file_via_dialog` Rust command performs the native Save-As
  dialog and the write together, so a compromised webview cannot forge that
  destination the way it could forge a plain IPC path argument.
- No regression to existing legitimate callers (saveCoordinator autosave,
  globalConfig.ts's config.json, captureCommit.ts, taskMutation.ts,
  useRenamePreview.ts) -- all continue to resolve to a workspace- or
  config-dir-contained absolute path, which the new gate allows.

Checkpoint: none needed; landed directly (see Landed below).

Landed: e2753ca on origin/main (branch agent/rm-dfd60513a2eb352c/b8d60ea2bbb0,
  pushed directly to main after the claim/renewal commits; confirmed an
  ancestor of origin/main by merge-base --is-ancestor).

Checks (this exact tree, after merging fresh main -- fast-forward, ROADMAP.md
only, no code conflict):
- `cargo fmt --check` (src-tauri): clean
- `cargo clippy --all-targets -- -D warnings` (src-tauri): 0 warnings
- `cargo test` (src-tauri): 83 passed, 0 failed (7 new: check_unscoped_write_allowed_*)
- `cargo check` (src-tauri): clean
- `npx tsc -p tsconfig.json --noEmit`: clean
- `npm run lint` (eslint .): clean
- `npm run check-version`: pass
- `npm test` (vitest run): 151 files, 2847 tests passed (net +19 vs. pre-change:
  7 Rust unit tests are separate; frontend added tests in tauriBridge.test.ts
  and tauriBridgeImpl.test.ts for the new native-mirror wiring and the new
  export/setActiveWorkspaceRoot bridge functions)
- `vite build` (npm run build): succeeds (pre-existing >500kB chunk warning,
  unrelated to this change)

Review: traced both success and failure paths. Real path: workspace note
overwrite (captureCommit/taskMutation/useRenamePreview/saveCoordinator) and
global config save both resolve to an absolute path the gate accepts, since
both still sit under the active workspace root or the app config dir;
verified this by inspection of every remaining `writeTextFile` call site
(grep across src/, confirmed none point outside those two roots). Failure
path: an arbitrary path (e.g. a symlink escaping the workspace, or a sibling
directory merely sharing a name prefix) is rejected before any filesystem
access, confirmed by dedicated unit tests. Did the revert-confirm discipline
on `check_unscoped_write_allowed` itself (temporarily made it always return
`Ok(())`): all 4 "rejects" tests failed as expected without the fix, and
passed again once restored -- see this session's own transcript for the
exact diff/output. Concurrency note found and corrected mid-session: an
earlier version of this change also touched
`src/workspace/capacitorBridgeImpl.ts`/`.test.ts` to satisfy a
platform-dispatcher type; that collided with a separate live claim
("Print/export a note on Android", hermes-local-20260922T123907Z-3f710899)
touching those exact files, so it was reverted and the native-workspace-root
mirror was rewired to call the desktop implementation directly
(`syncNativeActiveWorkspaceRoot` in `tauriBridge.ts`) instead of through the
shared `impl` dispatcher, avoiding that file entirely.

CI: hosted run https://github.com/LeonardSchwier/leotheca/actions/runs/35730341721
   for e2753ca was still in progress (all 4 jobs: android, backend,
   appimage-smoke, frontend early-stage in_progress) when this session ended
   verification and finished the item, per the verification-suite's "all
   applicable local checks and review pass; no known code failure -> push
   directly; a duplicate branch CI run is optional" landing rule. A later
   worker should confirm run 35730341721's conclusion for e2753ca and fix
   any real failure under this same item id if one appears.

Missing evidence: none for the changed behavior itself. Disclosed, deliberate
remaining scope (not required for this item, noted for a future session):
`write_text_file`/`write_binary_file`'s "active workspace root" state is
itself only as trustworthy as the frontend's own `activeWorkspaceRoot`
(the same characteristic every existing `*_workspace_*` command's
caller-supplied `workspace_root` argument already has) -- a deeper fix would
make the workspace root Rust-authoritative (set only via a real native
folder-picker dialog or independently re-derived from the app's own
Rust-written config), never trusting any frontend-supplied value at all.
Out of scope for this claim; recorded here rather than attempted partially.

Next action: none required; item is done. A future maintenance pass could
pursue the Rust-authoritative workspace-root hardening noted above as its
own separately-scoped item.
Retry: not applicable (finished, not blocked).
