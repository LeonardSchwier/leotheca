Task: rm-e0c2120bb902512b -- Maintenance review: `GraphView`'s and `CanvasView`'s open-file clicks are fire-and-forget, so a stale node/card silently does nothing instead of showing an error
Owner/token: ClaudeCode-cloud-scheduled-20260914T181147Z-9a5f681c, released (finished)
Scope: src/graph/GraphView.tsx, src/graph/GraphView.test.tsx, src/canvas/CanvasView.tsx, src/canvas/CanvasView.test.tsx, src/app/App.tsx (plus src/graph/graph.css, src/canvas/canvas.css for the new error styles, not separately claimed touch paths but within the same components)
Acceptance: both call sites await onOpenFile/handleOpenFile inside a try/catch and show an inline error instead of leaving an unhandled rejection; GraphView's panel-closing side effect in App.tsx does not fire before the open attempt is known to succeed; regression tests for both components proving the error path, revert-confirmed.
Checkpoint: none needed (landed directly)
Landed: 71c1c3b122ead618763cec1c5a6216949dc26c61 on main (fast-forward from claim 23019e9)
Checks: this exact tree -- npx tsc -p tsconfig.json --noEmit clean; npx eslint . 0 errors/0 warnings; npm run check-version pass; npx vitest run 2246/2246 passing (119 files, +4 new, no regressions); npx vite build pass; from src-tauri (bootstrapped this cloud sandbox's missing libwebkit2gtk-4.1-dev/libgtk-3-dev/librsvg2-dev/libayatana-appindicator3-dev/build-essential/libssl-dev/pkg-config): cargo fmt --all -- --check clean, cargo clippy --all-targets -- -D warnings clean, cargo test 64/64 passing (unchanged, no Rust touched), cargo check clean.
Review: same defect class already fixed in BacklinksPanel.tsx/DiagnosticsPanel.tsx/BookmarksPanel.tsx, found in two call sites those entries' own "out of scope" notes did not cover (TagsPanel/CollectionsPanel/FileTree were the only ones considered there). GraphView's nodes are canvas-drawn, not individual DOM elements, so a header banner (cleared on next attempt) serves the per-node-error role instead of a per-row message. CanvasView's existing `resolved` check only validates the stored filePath's shape, not that the file still exists, so this bug survived that check. App.tsx's GraphView onOpenFile previously closed the panel (graphOpen.value = false) before calling handleOpenFile; reordered to await first, matching SettingsPanel.tsx's handleSelectDiagnostic sequencing, so a failed open leaves the panel open with the error visible instead of closing anyway.
CI: hosted runs 34880285047 (CI) and 34880285597 (Release) were queued for 71c1c3b at finish time; a later check-in should confirm their conclusion and fix any real failure under this same understanding of the change.
Missing evidence: hosted CI conclusion for 71c1c3b not yet confirmed
Next action: check runs 34880285047 / 34880285597 for 71c1c3b; fix any real failure under a fresh claim of the same scope if one appears
Retry: available now (this is a completed item; only relevant if hosted CI later reveals a real failure)
