Task: rm-f25ffc80b950160e -- Maintenance review: the Markdown table toolbar's column/row commands use a strict `<` boundary against each cell's exclusive `sourceTo`, so a cursor resting immediately after a cell's trimmed text falls outside every detected cell/row range
Owner/token: ClaudeCode-20260915T062023Z-39de8999, released (finished)
Scope: src/markdown/tableCommands.ts, src/markdown/tableCommands.test.ts; resource markdown-table-commands
Acceptance: a cursor resting immediately after a cell's trimmed text (the ordinary position right after typing or clicking a word, before any trailing padding space and the delimiter pipe) resolves to the correct column for add-column-right/delete-column instead of silently no-op'ing, and resolves to the correct row for add-row-below instead of the blank row being spliced in at the table's top; new regression tests prove both, revert-confirmed to fail against the unfixed code.
Checkpoint: none needed (landed directly)
Landed: c0a44cf0fcbb95abac2f50205c375900d69f5ebf on main (fast-forward from claim 9ad9dce)
Checks: tsc -p tsconfig.json --noEmit clean; eslint 0 errors/0 warnings; npm run check-version pass; npx vitest run 119 files/2276 tests passing (+2 new, no regressions); npm run build pass. No Rust/native/bridge code touched (pure TS table-editing logic); RUST_CHECKS not in the applicable-checks table for this change per skills/verification-suite.md, not run.
Review: root cause traced to tables.ts's TableCell.sourceTo being an exclusive "position right after the last trimmed character" bound, combined with tableCommands.ts's strict `<` comparison against it. Confirmed reachable: MarkdownEditor.tsx's table-command effect calls tableEditAtCursor with the real CodeMirror view.state.selection.main.head. An existing test ("handles column operations at table boundaries") had computed its "cursorInPipe" via source.indexOf(" | ", ...), which lands on the space immediately after "Name" (the exact bug boundary), not the literal pipe character one position later, despite its own comment claiming "cursor in pipe character" -- it had encoded the bug as if intended. Corrected that test to actually land on the literal pipe (assertion stays null, still correct) with an explicit `source[cursorInPipe] === "|"` identity check, rather than weakening or removing it.
CI: run 34936772624 (CI) and run 34936772844 (Release) were queued for c0a44cf at finish time.
Missing evidence: hosted CI conclusion for c0a44cf not yet confirmed
Next action: check runs 34936772624 / 34936772844 for c0a44cf; fix any real failure under a fresh claim of the same scope if one appears
Retry: available now (this is a completed item; only relevant if hosted CI later reveals a real failure)
