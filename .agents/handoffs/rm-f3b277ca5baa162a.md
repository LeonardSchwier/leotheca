Task: rm-f3b277ca5baa162a -- F07 Phase 2b-persistence: wire live editor-layout state (pinned tabs, view mode) into workspace-settings persistence and restore it on workspace open
Owner/token: Claude-Code-cloud-scheduled-20260909T062215Z-3a8db7e7 / 24620bdf050b27ccbfe9e21744b25951
Scope: src/settings/store.ts, src/settings/store.test.ts, src/workspace/store.ts, src/workspace/store.test.ts (unused), src/workspace/documentGroups.ts, src/workspace/documentGroups.test.ts; resource editor-layout-persistence
Acceptance (final, corrected from the original plan once implementation showed EditorGroupState.viewMode has no live UI setter today):
  - Pinning/unpinning a tab persists to <workspace>/.leotheca/settings.json's editorLayout.groups.primary.pinnedPaths (same debounce/coalescing as the existing lastOpenPaths effect), not just the in-memory signal. DONE.
  - synchronizePrimaryEditorLayout preserves groups.primary.viewMode across an ordinary tab open/close instead of silently resetting it to "source" -- a real, if currently inert (no live per-group view-mode setter exists yet; that is Phase 3 scope), correctness fix needed so a restored view mode would actually survive the very next tab interaction once Phase 3 wires a setter. DONE, revert-confirmed.
  - On workspace open (initSettings cold start) and workspace switch (restoreLastOpenTabs), a persisted pin is restored once its tab reopens; a pinned path that no longer reads is skipped without blocking startup, matching restoreLastOpenTabs's existing tolerant handling. DONE, revert-confirmed.
  - Cold start (initSettings) previously loaded only the active tab; pinned tabs now also open eagerly (bounded set, pins are meant to be few) so they are visibly present after a restart, then focus is restored to the original active tab. DONE, revert-confirmed.
  - No behavior change to the secondary group (still always undefined; Phase 3 scope) or to ordinary non-pinned tab restore behavior.
  - Out of scope, left for a later phase: spec section 10.3's fuller redesign (restore all remembered tab labels without reading every note body, load content lazily on activation). Today's eager-load-what-you-open model is unchanged beyond adding pinned tabs to what cold start eagerly opens.
Checkpoint: none needed; verified and landing directly.
Landed: pending final integration push (see below).
Checks (this exact tree, 2026-09-09, after bootstrapping WebKitGTK/GTK3/appindicator system libraries for the Rust health check):
  - npx tsc --noEmit: clean.
  - npx vitest run: 113 files / 2102 tests passing (up from 2095; +7 new: 2 in documentGroups.test.ts's synchronizePrimaryEditorLayout/new restorePrimaryEditorLayout describe, 5 in settings/store.test.ts's new "editor-layout persistence" describe).
  - npx eslint .: 0 errors, 1 pre-existing warning (FileTree.tsx react-hooks/exhaustive-deps, unrelated, not gated by this repo's lint policy).
  - npm run check-version: pass.
  - npx vite build: pass (pre-existing chunk-size warning, unrelated).
  - cargo fmt --check / cargo check / cargo clippy --all-targets -- -D warnings / cargo test (57/57): all green from src-tauri (no Rust touched; run as a health check).
  - Revert-confirmed: stashed src/workspace/documentGroups.ts alone and reran the new viewMode-preservation test -- fails for the right reason (source instead of preview) with the fix reverted. Separately stashed src/settings/store.ts + src/workspace/store.ts and reran the new "editor-layout persistence" tests -- 3 of them fail for the right reason (no persisted write; pins not restored; pinned tab not eagerly opened) with the fix reverted.
Review: Traced the real success path (pin a tab -> effect persists editorLayout -> restart -> initSettings/restoreLastOpenTabs reads persisted pins -> eagerly reopens/reapplies them) and the real failure path (pinned file deleted between sessions -> readTextFile throws -> path skipped, never marked pinned, no blocked startup). Found and fixed a genuine regression the new 4-field persistence key surfaced: setWorkspacePath's/forgetWorkspaceProfile's three `lastPersistedTabsKey = JSON.stringify([[], null])` preseeds (before an internal closeAllTabs() during a workspace transition) still used the old 2-element key shape; since synchronizePrimaryEditorLayout now preserves viewMode instead of resetting it, a hardcoded default no longer reliably matched post-close state, which reintroduced the exact spurious-write bug the preseed exists to prevent. Fixed by computing the preseed from the current live viewMode instead of hardcoding it. Also found and fixed pre-existing test-order fragility this surfaced: several tests call closeAllTabs() before (not after) nulling workspacePath/settingsLoaded, which is safe only while the persistence effect's key ignores pin/view-mode state; once pin state is tracked, a live effect left enabled by an earlier test's leftover workspacePath/settingsLoaded can fire on that closeAllTabs() and clobber a later test's own just-assigned workspaceSettings.value before it's read. Fixed by reordering the reset (null workspacePath/settingsLoaded, then closeAllTabs) in the affected tests, including one pre-existing "settings hydration" test.
CI: no hosted CI run consulted for this exact branch; landing rests on the exhaustive local verification above, per this repo's own documented policy that full local coverage permits direct landing.
Missing evidence: section 10.3's fuller "restore without reading every note body" redesign is not attempted (out of scope, disclosed above); no on-device/real-desktop manual smoke test of restart behavior was performed in this sandbox (no interactive session available), so the fix's correctness rests on the automated regression tests and hand-traced logic above, not a real click-through.
Next action: none; complete and ready to finish once pushed and confirmed on main.
Retry: n/a (not blocked).
