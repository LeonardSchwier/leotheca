# Handoff — rm-f506e6522fb79cba: Offline spellcheck linter permanently dead after pre-dictionary first pass

**Agent**: hermes-local-20260921T203758Z-b20dabd6
**Token**: 38fd0dbfb2cdf6f88cffed4b4a04c77e
**Base**: efc125f (origin/main at claim time)
**Work branch**: `agent/rm-f506e6522fb79cba/38fd0dbfb2cd`
**Implementation commit**: `0dd3849531d32dcec5b62f7a99eb64b1c1d8d88b`
**Status**: implemented, locally verified, checkpoint pushed, ready to land

## Summary

The CodeMirror linter extension created for the offline spellcheck feature
(`spellCheckExtension` in `src/spellcheck/spellCheck.ts`) cached a `null`
checker permanently on its first lint pass. If that first pass fired before
the editor's `useEffect` had populated the dictionary refs (`.aff`/`.dic`
content), the linter silently produced `[]` for the entire lifetime of the
view — even after the dictionary had loaded. Spellchecking was therefore
permanently dead for any editor that ran its first lint pass during the
load window.

## Acceptance criteria

1. `spellCheckExtension` returns a linter whose source, when the first
   `createChecker()` call returns `null` (dictionary not yet loaded) and a
   later call returns a working checker (dictionary now loaded), produces
   diagnostics on the later pass. (This is the core regression.)
2. The "missing dictionary → silently off" contract is preserved: if
   `createChecker()` keeps returning `null` (files genuinely absent), the
   linter keeps returning `[]` on every pass without throwing or attempting
   a real nspell parse.
3. A successful checker is still cached, so the common case keeps the single
   nspell parse (no per-lint-pass dictionary re-parse).
4. Two regression tests encode (1) and (2). Both fail on the pre-fix code
   (proven by stashing the fix and re-running) and pass on the fixed code.

## Root cause

`getChecker()` used a `checkerChecked` boolean latch:

```ts
let cachedChecker: SpellChecker | null = null;
let checkerChecked = false;
function getChecker(): SpellChecker | null {
  if (!checkerChecked) {
    checkerChecked = true;          // ← latched on first pass
    try { cachedChecker = createChecker(); }
    catch { cachedChecker = null; }
  }
  return cachedChecker;
}
```

Once `checkerChecked` was `true`, the checker was never re-derived. A first
pass that saw empty dictionary refs → `null` cached forever.

## Fix

Re-derive the checker on every pass while the cached value is `null`; cache
the first successful result:

```ts
let cachedChecker: SpellChecker | null = null;
function getChecker(): SpellChecker | null {
  if (cachedChecker) return cachedChecker;
  try {
    const next = createChecker();
    if (next) cachedChecker = next;   // success → cache it
    return next;                      // null → not cached; retried next pass
  } catch {
    return null;                      // transient throw; retried next pass
  }
}
```

This preserves the "missing dictionary → silently off" contract (the files
either appear — ref populated → `createChecker()` returns a checker, or they
don't — `createChecker()` keeps returning `null` quickly via its empty-dic
guard, no nspell parse). It also survives a transient first-pass throw.

## Regression evidence

The two new tests extract the linter source from the extension
(`spellCheckExtension(...)[0][0].value.source`) and drive it with a fake
`{ state: EditorState.create({ doc }) }` view (the source only reads
`view.state.doc`).

- Test A (the race): a `createChecker` stub that returns `null` on pass 1
  and a working `mockChecker` on pass 2+. Asserts pass 1 → `[]`, pass 2 →
  diagnostics for "teh"/"quik" but not "brown".
  - **Pre-fix**: `FAIL — expected [] to include 'Misspelled word: teh'`
    (the cached null was returned on pass 2).
  - **Post-fix**: `PASS`.
- Test B (silently off): a `createChecker` stub that always returns `null`.
  Asserts every pass → `[]` and the stub is called on every pass (3 calls for
  3 passes) — proving re-derivation while null, no crash.
  - **Pre-fix**: `FAIL — expected 1 to be 3` (only 1 call; the latch).
  - **Post-fix**: `PASS`.

Proof method: `git stash push src/spellcheck/spellCheck.ts` (fix reverted),
ran `npx vitest run src/spellcheck/spellCheck.test.ts -t race` → both new
tests failed; `git stash pop` (fix restored) → both pass.

## Test results (all on the exact working tree)

- `npx vitest run src/spellcheck/` — **18/18 pass** (was 16, +2 new)
- `npm test` (full `vitest run`) — **24911 passed, 1319 files**
- `npx tsc -p tsconfig.json --noEmit` — **clean**
- `npm run lint` (`eslint .`) — **clean**
- `npm run check-version` (`node scripts/checkVersion.js`) — **pass**
- `npm run build` (`tsc -p tsconfig.json --noEmit && vite build`) — **EXIT=0, built in 4.6s**

## Self-review findings

1. The fix is minimal and surgical: it only changes the cache-control logic
   in `getChecker`; the checker creation, `lintSource`, and diagnostic
   mapping are untouched.
2. Verified the "missing dictionary → silently off" contract is preserved,
   not just the happy path — Test B asserts every pass returns `[]` and the
   stub is called each pass (re-derivation) without any nspell parse.
3. Confirmed the successful-checker caching is retained (no per-pass nspell
   re-parse) by code inspection: `if (next) cachedChecker = next;` then
   `if (cachedChecker) return cachedChecker;` short-circuits later passes.
4. No other code path depends on the removed `checkerChecked` flag — it was
   module-private to `spellCheckExtension`.
5. The two tests extract the real linter source from the extension rather
   than re-implementing the logic, so they exercise the actual shipped code.

## Independent review

Not performed in this session — this is a self-review only. The change is
small, isolated to one function's cache control, and covered by two
failing-then-passing regression tests plus the full existing suite. If an
independent reviewer is available in a later worker, focus on: whether the
re-derivation-while-null behavior has any observable cost (it only re-calls
`createChecker`, which short-circuits on empty dictionary content — no nspell
parse).

## Result

**LANDED.** Fix is on `main`.

- Landed SHA (main HEAD): `83329f0b927c5db502875d6cddb8c596b5388166`
  - Merge of work branch: `7a5d326` (implementation + handoff + claim/finish metadata)
  - Format/dedup fix: `83329f0` (de-duplicated the merge's duplicate entry, shortened
    the 672-char visible summary to 352 chars to satisfy the 500-char budget)
- Work branch checkpoint: `9beddfe` (implementation `0dd3849` + handoff)
- CI: `ledger` (Agent policy roadmap-format gate) **passes** on `83329f0`.
  The full CI suite (frontend/backend/platform builds) was not yet visible in
  the GitHub API at handoff time (same multi-minute lag seen on prior commits).
  Local verification is complete and green (see above).

## Post-landing note

The initial push (`7a5d326`) left a duplicate: the `ort` merge kept the work
branch's open (⬜) copy AND the control tree's done (✅) copy, and both had a
672-char visible summary over the 500-char budget, which failed the Agent
policy "roadmap format" gate. Fixed in `83329f0` (single ✅ entry, 352-char
summary). `ledger` check now passes.

## Next command

None — task complete, landed, and the blocking CI gate (ledger) is green.
If the full CI suite shows a failure in a later worker, investigate and fix
on main. The application code is byte-identical to the verified work branch.
