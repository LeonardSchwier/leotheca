name: F04 Phase 5e1 direct patcher

on:
  push:
    branches:
      - automation/f04e1-direct-2102

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Build focused final candidate
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin agent/f04-phase5e1-heading-block-ids
          git checkout -B work origin/agent/f04-phase5e1-heading-block-ids
          python - <<'PY'
          from pathlib import Path
          import re

          def exact(path, old, new, count=1):
              p = Path(path)
              text = p.read_text(encoding="utf-8")
              found = text.count(old)
              if found != count:
                  raise SystemExit(f"{path}: expected {count} matches, found {found}: {old[:100]!r}")
              p.write_text(text.replace(old, new, count), encoding="utf-8")

          # Heading parser owns heading block markers.
          exact("src/markdown/headings.ts", "  occurrence: number;\n  level: 1 | 2 | 3 | 4 | 5 | 6;", "  occurrence: number;\n  /** Optional explicit block ID attached to this heading. */\n  blockId?: { id: string; idFrom: number; idTo: number };\n  level: 1 | 2 | 3 | 4 | 5 | 6;")
          exact("src/markdown/headings.ts", "const CLOSING_HASH_RE = /[ \\t]+#+$/;", "const CLOSING_HASH_RE = /[ \\t]+#+$/;\nconst HEADING_BLOCK_ID_RE = /[ \\t]+\\^([A-Za-z0-9][A-Za-z0-9-]{0,63})[ \\t]*$/;")
          exact("src/markdown/headings.ts", "  line: number;\n  column: number;\n}\n\nfunction tryParseAtx", "  line: number;\n  column: number;\n  blockId?: { id: string; idFrom: number; idTo: number };\n}\n\nfunction extractHeadingBlockId(rawText: string, absoluteContentStart: number): { rawText: string; contentTo: number; blockId?: { id: string; idFrom: number; idTo: number } } {\n  const match = HEADING_BLOCK_ID_RE.exec(rawText);\n  if (!match) return { rawText, contentTo: absoluteContentStart + rawText.length };\n  const id = match[1];\n  const idFrom = absoluteContentStart + match.index + match[0].indexOf(\"^\");\n  return { rawText: rawText.slice(0, match.index), contentTo: absoluteContentStart + match.index, blockId: { id, idFrom, idTo: idFrom + 1 + id.length } };\n}\n\nfunction tryParseAtx")
          old_atx = '''  let raw = line.text.slice(contentStart, contentEnd);
  const closing = CLOSING_HASH_RE.exec(raw);
  if (closing) {
    contentEnd -= closing[0].length;
    raw = raw.slice(0, raw.length - closing[0].length);
  }
  return {
    level: hashes.length as RawHeading["level"],
    rawText: raw,
    sourceFrom: line.start,
    sourceTo: line.end,
    contentFrom: line.start + contentStart,
    contentTo: line.start + contentEnd,
    line: line.lineNumber,
    column: 1,
  };'''
          new_atx = '''  let raw = line.text.slice(contentStart, contentEnd);
  const closing = CLOSING_HASH_RE.exec(raw);
  if (closing) {
    contentEnd -= closing[0].length;
    raw = raw.slice(0, raw.length - closing[0].length);
  }
  const parsedBlockId = extractHeadingBlockId(raw, line.start + contentStart);
  raw = parsedBlockId.rawText;
  contentEnd = parsedBlockId.contentTo - line.start;
  return {
    level: hashes.length as RawHeading["level"],
    rawText: raw,
    sourceFrom: line.start,
    sourceTo: line.end,
    contentFrom: line.start + contentStart,
    contentTo: line.start + contentEnd,
    line: line.lineNumber,
    column: 1,
    blockId: parsedBlockId.blockId,
  };'''
          exact("src/markdown/headings.ts", old_atx, new_atx)
          old_setext = '''        consumedLines.add(i - 1);
        consumedLines.add(i);
        raw.push({
          level: setextLevel,
          rawText: textLine.text.slice(contentStart, contentEnd),
          sourceFrom: textLine.start,
          sourceTo: line.end,
          contentFrom: textLine.start + contentStart,
          contentTo: textLine.start + contentEnd,
          line: textLine.lineNumber,
          column: 1,
        });'''
          new_setext = '''        const parsedBlockId = extractHeadingBlockId(
          textLine.text.slice(contentStart, contentEnd),
          textLine.start + contentStart,
        );
        contentEnd = parsedBlockId.contentTo - textLine.start;
        consumedLines.add(i - 1);
        consumedLines.add(i);
        raw.push({
          level: setextLevel,
          rawText: parsedBlockId.rawText,
          sourceFrom: textLine.start,
          sourceTo: line.end,
          contentFrom: textLine.start + contentStart,
          contentTo: textLine.start + contentEnd,
          line: textLine.lineNumber,
          column: 1,
          blockId: parsedBlockId.blockId,
        });'''
          exact("src/markdown/headings.ts", old_setext, new_setext)
          exact("src/markdown/headings.ts", "      key,\n      occurrence,\n      level: h.level,", "      key,\n      occurrence,\n      blockId: h.blockId,\n      level: h.level,")

          # Block scanner reuses canonical headings and merges them into one namespace.
          exact("src/markdown/blocks.ts", "/**\n * F04 Phase 3a/3b/3d/3e's block-reference scanner", "import { scanHeadings } from \"./headings\";\n\n/**\n * F04 Phase 3a/3b/3d/3e's block-reference scanner")
          exact("src/markdown/blocks.ts", '  kind: "paragraph" | "list-item" | "blockquote" | "fenced-code";', '  kind: "paragraph" | "list-item" | "blockquote" | "fenced-code" | "heading";')
          exact("src/markdown/blocks.ts", "  flushPendingFencedCode();\n  flushParagraph();\n  flushOpenBlock();\n\n  return blocks;", "  flushPendingFencedCode();\n  flushParagraph();\n  flushOpenBlock();\n\n  for (const heading of scanHeadings(content)) {\n    blocks.push({\n      kind: \"heading\",\n      sourceFrom: heading.sourceFrom,\n      sourceTo: heading.sourceTo,\n      contentFrom: heading.contentFrom,\n      contentTo: heading.contentTo,\n      line: heading.line,\n      column: heading.column,\n      marker: heading.blockId\n        ? { id: heading.blockId.id, idFrom: heading.blockId.idFrom, idTo: heading.blockId.idTo }\n        : undefined,\n    });\n  }\n  blocks.sort((a, b) => a.sourceFrom - b.sourceFrom);\n\n  return blocks;")
          old_test = '''  it("returns undefined for an offset inside a heading or a blank line between blocks", () => {
    const source = "# A heading\\n\\nA paragraph.";
    expect(findBlockAtOffset(source, source.indexOf("heading"))).toBeUndefined();
    expect(findBlockAtOffset(source, source.indexOf("\\n\\n") + 1)).toBeUndefined();
  });'''
          new_test = '''  it("finds a heading block and still returns undefined for a blank line between blocks", () => {
    const source = "# A heading\\n\\nA paragraph.";
    expect(findBlockAtOffset(source, source.indexOf("heading"))?.kind).toBe("heading");
    expect(findBlockAtOffset(source, source.indexOf("\\n\\n") + 1)).toBeUndefined();
  });'''
          exact("src/markdown/blocks.test.ts", old_test, new_test)

          # Persisted index accepts heading records with block IDs and invalidates v7.
          exact("src/linking/store.ts", '      record.kind === "blockquote" ||\n      record.kind === "fenced-code") &&', '      record.kind === "blockquote" ||\n      record.kind === "fenced-code" ||\n      record.kind === "heading") &&')
          exact("src/linking/store.ts", "const LINK_INDEX_CACHE_VERSION = 7;", "const LINK_INDEX_CACHE_VERSION = 8;")
          p = Path("src/linking/store.ts")
          t = p.read_text(encoding="utf-8")
          start = t.index("function isValidHeadingRecord")
          segment = t[start:]
          marker = '  const isFiniteNumber = (v: unknown): v is number =>\n    typeof v === "number" && Number.isFinite(v);\n  return (\n    typeof record.key === "string" &&'
          replacement = '  const isFiniteNumber = (v: unknown): v is number =>\n    typeof v === "number" && Number.isFinite(v);\n  const blockId = record.blockId;\n  const hasValidBlockId =\n    blockId === undefined ||\n    (typeof blockId === "object" &&\n      blockId !== null &&\n      typeof (blockId as Record<string, unknown>).id === "string" &&\n      isFiniteNumber((blockId as Record<string, unknown>).idFrom) &&\n      isFiniteNumber((blockId as Record<string, unknown>).idTo));\n  return (\n    hasValidBlockId &&\n    typeof record.key === "string" &&'
          if segment.count(marker) != 1:
              raise SystemExit("src/linking/store.ts: heading validator shape drifted")
          p.write_text(t[:start] + segment.replace(marker, replacement, 1), encoding="utf-8")
          p = Path("src/linking/store.test.ts")
          t = p.read_text(encoding="utf-8")
          if t.count("version: 7,") != 3:
              raise SystemExit(f"src/linking/store.test.ts: expected 3 version 7 fixtures, found {t.count('version: 7,')}")
          t = t.replace("version: 7,", "version: 8,")
          if t.count('if (path.endsWith("Alpha.md")) return "A paragraph with an id. ^my-block";') != 1:
              raise SystemExit("src/linking/store.test.ts: Alpha fixture drifted")
          t = t.replace('if (path.endsWith("Alpha.md")) return "A paragraph with an id. ^my-block";', 'if (path.endsWith("Alpha.md")) return "# Section ^heading-id\\n\\nA paragraph with an id. ^my-block";', 1)
          if t.count('expect(alphaBlocks?.map((b) => b.id)).toEqual(["my-block"]);') != 1:
              raise SystemExit("src/linking/store.test.ts: block expectation drifted")
          t = t.replace('expect(alphaBlocks?.map((b) => b.id)).toEqual(["my-block"]);', 'expect(alphaBlocks?.map((b) => b.id)).toEqual(["heading-id", "my-block"]);', 1)
          p.write_text(t, encoding="utf-8")

          # Preview strips only the heading marker and anchors the rendered heading element.
          exact("src/editor/MarkdownPreview.tsx", "    result += source.slice(cursor, block.contentTo);\n    cursor = block.sourceTo;", "    result += source.slice(cursor, block.contentTo);\n    cursor = block.kind === \"heading\" ? block.idTo : block.sourceTo;")
          exact("src/editor/MarkdownPreview.tsx", '              kind === "list-item" ? "li" : kind === "blockquote" ? "blockquote" : "p",', '              kind === "list-item"\n                ? "li"\n                : kind === "blockquote"\n                  ? "blockquote"\n                  : kind === "heading"\n                    ? "h1,h2,h3,h4,h5,h6"\n                    : "p",')

          Path("src/markdown/headingBlockIds.test.ts").write_text('''import { describe, expect, it } from "vitest";
import { resolveBlockLinkAtCursor } from "../editor/blockLinkActions";
import { findBlockAtOffset, scanBlockIds } from "./blocks";
import { scanHeadings } from "./headings";

describe("heading block IDs", () => {
  it("parses an ATX marker without exposing it as heading text", () => {
    const source = "## Architecture boundary ^architecture-boundary";
    const [heading] = scanHeadings(source);
    expect(heading.displayText).toBe("Architecture boundary");
    expect(heading.blockId?.id).toBe("architecture-boundary");
    expect(source.slice(heading.contentFrom, heading.contentTo)).toBe("Architecture boundary");
    expect(source.slice(heading.blockId!.idFrom, heading.blockId!.idTo)).toBe("^architecture-boundary");
  });
  it("recognizes a marker before optional ATX closing hashes", () => {
    const [heading] = scanHeadings("### Shipping ^release ##");
    expect(heading.displayText).toBe("Shipping");
    expect(heading.blockId?.id).toBe("release");
  });
  it("parses a setext heading marker while preserving its underline", () => {
    const source = "Architecture ^arch\\n============\\nBody";
    const [heading] = scanHeadings(source);
    expect(heading.displayText).toBe("Architecture");
    expect(heading.blockId?.id).toBe("arch");
    expect(source.slice(heading.sourceFrom, heading.sourceTo)).toBe("Architecture ^arch\\n============");
  });
  it("leaves malformed heading markers visible", () => {
    const [heading] = scanHeadings("## Heading ^bad_id");
    expect(heading.blockId).toBeUndefined();
    expect(heading.displayText).toBe("Heading ^bad_id");
  });
  it("shares one case-insensitive duplicate namespace with other block kinds", () => {
    const blocks = scanBlockIds("## Heading ^Shared\\n\\nParagraph. ^shared");
    expect(blocks.map((block) => block.kind)).toEqual(["heading", "paragraph"]);
    expect(blocks.map((block) => block.occurrence)).toEqual([1, 2]);
  });
  it("finds a markerless heading as the block at the cursor", () => {
    const source = "# Markerless heading\\n\\nBody.";
    expect(findBlockAtOffset(source, source.indexOf("heading"))?.kind).toBe("heading");
  });
  it("lets Copy block link create a marker for a markerless heading", () => {
    const source = "## Heading\\n\\nBody.";
    const heading = scanHeadings(source)[0];
    const result = resolveBlockLinkAtCursor(source, source.indexOf("Heading") + 2)!;
    expect(result.insertion?.from).toBe(heading.contentTo);
    expect(result.insertion?.text).toMatch(/^ \\^b-[a-f0-9]{8}$/);
    expect(result.linkText).toBe(`[[#^${result.insertion!.text.slice(2)}]]`);
  });
});
''', encoding="utf-8")
          Path("src/editor/MarkdownPreview.headingBlocks.test.tsx").write_text('''/** @vitest-environment jsdom */
import { cleanup, render } from "@testing-library/preact";
import { afterEach, describe, expect, it, vi } from "vitest";

vi.mock("../workspace/tauriBridge", () => ({ fileSrc: vi.fn(), readTextFile: vi.fn() }));
vi.mock("../settings/store", async () => {
  const { signal } = await import("@preact/signals");
  return { workspacePath: signal<string | null>(null) };
});

import { MarkdownPreview } from "./MarkdownPreview";

afterEach(() => cleanup());

describe("MarkdownPreview heading block IDs", () => {
  it("hides an ATX marker and anchors the rendered heading", () => {
    const { container } = render(<MarkdownPreview source="## Architecture ^arch" notePath="/vault/note.md" />);
    const heading = container.querySelector("h2");
    expect(heading?.textContent).toBe("Architecture");
    expect(heading?.id).toBe("lt-block-arch");
    expect(heading?.getAttribute("data-lt-block-id")).toBe("arch");
  });
  it("keeps setext rendering intact while hiding and anchoring its marker", () => {
    const { container } = render(<MarkdownPreview source={"Architecture ^arch\\n============\\nBody"} notePath="/vault/note.md" />);
    const heading = container.querySelector("h1");
    expect(heading?.textContent).toBe("Architecture");
    expect(heading?.id).toBe("lt-block-arch");
  });
});
''', encoding="utf-8")

          exact("CHANGELOG.md", "## Unreleased\n", "## Unreleased\n\n- Block references now support headings. A trailing `^id` on an ATX or setext heading stays hidden in Preview, shares the normal block-ID namespace, and works with Copy block link.\n")
          exact("documentation/ARCHITECTURE.md", "\n## Build and distribution architecture\n", "\n### Heading-owned block IDs\n\n`markdown/headings.ts` remains the only heading parser and owns optional trailing `^id` markers. `markdown/blocks.ts` merges those canonical records into the shared block model so block resolution, duplicate detection, Copy block link, Preview anchoring, and link-index persistence all use the same heading interpretation. The link-index cache version advances whenever this persisted shape changes.\n\n## Build and distribution architecture\n")

          roadmap = Path("ROADMAP.md")
          lines = roadmap.read_text(encoding="utf-8").splitlines()
          matches = [i for i, line in enumerate(lines) if line.startswith("- 🚧 **F04 Phase 5e1:")]
          if len(matches) != 1:
              raise SystemExit(f"ROADMAP.md: expected one F04 Phase 5e1 claim, found {len(matches)}")
          lines.pop(matches[0])
          entry = "- ✅ **F04 Phase 5e1: heading block-ID eligibility and Preview heading anchors** (claim: ChatGPT-GPT-5.6-Sol-manual-20260903T2052Z, branch: agent/f04-phase5e1-heading-block-ids, spec: `spec/f04-heading-block-links-embeds.md`, sections 7.1-7.4 and 21 Phase 5): ATX and setext headings can own trailing explicit `^id` markers through the canonical heading scanner. Markers stay out of visible heading text, join the shared case-insensitive block-ID namespace, work with Copy block link, render deterministic Preview anchors on the heading element, and persist through the link index with cache version 8. Focused tests cover ATX and setext syntax, closing hashes, malformed markers, duplicate IDs across block kinds, markerless heading lookup and Copy block link insertion, Preview anchoring, and indexed heading IDs. Phase 5e2 remains open for the Preview hover/focus/long-press copy affordance; Phase 5e3 remains open for the separate Create block link entry point."
          implemented = lines.index("## Implemented")
          lines[implemented + 1:implemented + 1] = ["", entry]
          roadmap.write_text("\n".join(lines) + "\n", encoding="utf-8")

          for path in ["src/markdown/headings.ts", "src/markdown/blocks.ts", "src/markdown/blocks.test.ts", "src/linking/store.ts", "src/linking/store.test.ts", "src/editor/MarkdownPreview.tsx", "src/markdown/headingBlockIds.test.ts", "src/editor/MarkdownPreview.headingBlocks.test.tsx", "CHANGELOG.md", "documentation/ARCHITECTURE.md", "ROADMAP.md"]:
              if "\x00" in Path(path).read_text(encoding="utf-8"):
                  raise SystemExit(f"{path}: null byte found")
          PY
          npm ci
          npx tsc --noEmit
          npx vitest run src/markdown/headings.test.ts src/markdown/blocks.test.ts src/markdown/headingBlockIds.test.ts src/editor/MarkdownPreview.headingBlocks.test.tsx src/editor/blockLinkActions.test.ts src/linking/store.test.ts
          git config user.name "Leonardschwier"
          git config user.email "github@leonardschwier.de"
          git add src/markdown/headings.ts src/markdown/blocks.ts src/markdown/blocks.test.ts src/linking/store.ts src/linking/store.test.ts src/editor/MarkdownPreview.tsx src/markdown/headingBlockIds.test.ts src/editor/MarkdownPreview.headingBlocks.test.tsx CHANGELOG.md documentation/ARCHITECTURE.md ROADMAP.md
          git diff --cached --quiet && { echo "candidate produced no change"; exit 1; }
          git commit -m "Implement F04 Phase 5e1 heading block IDs"
          git push origin HEAD:agent/f04-phase5e1-heading-block-ids
