name: Capture exact F09 TypeScript diagnostics

on:
  push:
    branches:
      - agent/f09-phase1-collections
    paths:
      - .github/workflows/f09-capture-tsc.yml

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Capture exact compiler diagnostics
        shell: bash
        run: |
          set -euo pipefail
          git config user.name Leonardschwier
          git config user.email github@leonardschwier.de
          npm ci
          set +e
          npx tsc -p tsconfig.json --noEmit > .f09-tsc.txt 2>&1
          status=$?
          set -e
          printf '\nexit=%s\n' "$status" >> .f09-tsc.txt
          git add .f09-tsc.txt
          git rm -f .f09-typecheck.txt .github/workflows/f09-capture-tsc.yml
          git commit -m "Capture exact F09 TypeScript diagnostics"
          git push origin HEAD:agent/f09-phase1-collections
