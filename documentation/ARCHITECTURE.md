# Solution architecture

This document explains how Leotheca is put together and why, for anyone (human or AI agent) making a non-trivial change. It complements [`CONSTITUTION.md`](../CONSTITUTION.md) (binding rules and decisions) and [`CONTRIBUTING.md`](../CONTRIBUTING.md) (how to get a dev environment running); this file is about structure, not rules or setup steps.

## System overview

Leotheca is a local-first markdown editor with two native shells sharing one frontend:

```
                    +-----------------------------+
                    |   Shared frontend (src/)     |
                    |   Preact + TypeScript +      |
                    |   CodeMirror 6                |
                    +---------------+---------------+
                                    |
                     src/workspace/tauriBridge.ts
                     (platform dispatcher, see below)
                                    |
              +---------------------+---------------------+
              |                                             |
   +----------v----------+                       +----------v-----------+
   |  Tauri (Linux)       |                       |  Capacitor (Android) |
   |  src-tauri/ (Rust)   |                       |  android/ (Java) +   |
   |  real filesystem     |                       |  custom SAF plugins  |
   +-----------------------+                       +-----------------------+
```

There is no backend, no server, no account system. A "workspace" is a folder of `.md` files the user points the app at; everything the app does is read/write operations against that folder plus a small amount of local app configuration. See `PHILOSOPHY.md` for why this is a hard constraint, not just a current implementation detail.

## Optional Web Clipper

`extensions/web-clipper/` is a standalone WebExtension, deliberately outside the application bundles and platform bridges. Its content script converts only the user's current selection into Markdown text, then asks the extension background worker to open the browser's normal save dialog for a `.md` download. The user chooses the workspace folder in that dialog. This keeps the extension from receiving broad filesystem access or any application-to-extension channel, and it adds no network call: it works with content the browser has already displayed. `clipperCore.js` owns the text conversion and has DOM-level tests for empty selections, unsafe links, and ordinary structure; the remaining scripts only connect that deterministic conversion to selection, popup, shortcut, and download APIs.

## The platform abstraction layer

Every file operation in the frontend goes through `src/workspace/tauriBridge.ts`, a thin dispatcher:

```typescript
const impl = Capacitor.isNativePlatform() ? android : desktop;
export const listDir = impl.listDir;
export const readTextFile = impl.readTextFile;
// ...
```

`desktop` is `tauriBridgeImpl.ts` (calls into the Rust commands in `src-tauri/src/commands.rs` and `src-tauri/src/workspace_mutations.rs` via Tauri's `invoke`). `android` is `capacitorBridgeImpl.ts` (calls into the custom `FolderAccessPlugin`/`WorkspaceMutationPlugin` described below). Every other module in the frontend imports from `tauriBridge.ts` and is written as if there were one platform; the dispatcher is the only place that needs to know two exist. When adding a new file operation, add it to both `*Impl.ts` files and re-export it from `tauriBridge.ts`, not to call sites directly.

Every mutation that changes content already inside an open workspace, including debounced note autosave, creating or writing a note, canvas, template copy, or attachment, creating a folder, renaming, and permanently deleting, goes through a workspace-scoped bridge function rather than the older plain `write/create/rename/deletePath` ones (audit follow-up F-004). The distinction is containment and mutation semantics, not naming. `resolvePathWithinWorkspace(workspacePath.value, absolutePath)` from `workspace/paths.ts`, or the caller's own already-known root, supplies a workspace-relative path. On desktop, `commands.rs`'s `resolve_within_workspace` canonicalizes it and verifies the result is genuinely inside the workspace root before any `std::fs` call runs. Creates and renames that must not replace an existing path use the dedicated commands in `workspace_mutations.rs`, which perform the collision decision at the native boundary instead of relying on a frontend list-then-mutate preflight. The older plain write functions remain only for app-level state that has no workspace to contain against, such as `settings/globalConfig.ts`.

On Android, containment is enforced by the active Storage Access Framework tree grant rather than POSIX canonicalization. `capacitorBridgeImpl.ts` resolves the parent inside that already-granted tree and sends mutation calls to `WorkspaceMutationPlugin`, a subclass registered under the existing `FolderAccess` bridge name so the same native capability owns the workspace URI, path cache, and mutations. The plugin validates a mutation name as one path segment, rejects an already-existing target for create operations, verifies a provider-created name was not silently uniquified, and uses `DocumentsContract.renameDocument` with an explicit sibling collision check for rename. `src/workspace/capacitorMutationContract.test.ts` exercises the frontend/native contract and failure mapping. This is a different mechanism from desktop canonicalization, but both platforms now enforce their workspace mutation contract at the native boundary instead of trusting a caller-computed absolute path.

`resolve_within_workspace`'s symlink policy: a symlink anywhere in the resolved path (an intermediate directory, or the exact target itself) is followed only if its real, canonicalized location is inside the workspace root; one resolving outside is rejected, and so is a dangling symlink sitting exactly at the target (it has no canonical form to check, and would otherwise silently look like "doesn't exist yet" to a naive check). Containment is checked component-wise (`Path::starts_with`), not by string prefix, so a workspace at `/vault` never accepts a path that resolves to a sibling like `/vault-evil` just because the string happens to start the same way. `src-tauri/src/commands.rs`'s own test module has the fixtures for all of this, including the exact bug shapes an audit or a future refactor could otherwise reintroduce.

Desktop write replacement is crash-safe: workspace writes and the remaining app-level plain writes use the atomic write helper, which writes a sibling temporary file before renaming it over the destination. `saveCoordinator.ts` now captures the workspace root with each `{session, path}` entry and routes note-content autosave through `writeWorkspaceTextFile`; the root is threaded through the coordinator rather than imported from `settings/store.ts`, avoiding the circular import that originally blocked this migration. Together with the transition drain below, a queued save keeps the workspace identity it was created for and cannot be redirected merely because the current settings signal has changed.

macOS and Windows (both real build targets now, see "Desktop shell: Tauri" below) reuse `tauriBridgeImpl.ts` as-is, unlike Android: they're both still the same Tauri desktop shell talking to the same real filesystem via `std::fs`, just a different OS underneath, so no third `*Impl.ts` file was needed. The one real cross-platform wrinkle this surfaced: `Path::to_string_lossy()` returns backslash-separated paths on Windows, while every path this project already handles (`dirname`, `relativePath`, wikilink resolution, `workspace/paths.ts`) assumes a forward-slash string join is safe, true only while Linux and Android (both forward-slash) were the only targets. Fixed once, in `commands.rs`'s `path_to_string`, at the one place every path crosses on its way to the frontend, rather than teaching every consumer about `\`.

## Workspace transitions: an authoritative coordinator

Switching workspaces (picking a new folder on desktop, or a new SAF grant on Android) is not a single atomic step: it involves draining outgoing async work, activating a new native grant, loading that workspace's settings, and only then publishing new UI state. Before audit follow-up N-001/N-003, this sequencing lived as ad hoc effects in `App.tsx` and a `resetForSession` call that filtered the wrong session, so an old workspace's debounce timers could survive into a new one and an in-flight native write could not always be safely orphaned when an Android grant changed underneath it.

`src/workspace/workspaceTransition.ts` now owns this as an explicit state machine: `createWorkspaceTransitionCoordinator()` hands out a monotonically increasing generation on every `run()` call, and each of its named steps (`prepareOutgoing`, `connectIncoming`, `loadIncoming`, `publishIncoming`, `publishFailure`, `afterPublish`) re-checks whether its generation is still current before doing anything observable. A later `setWorkspacePath` call always wins over an earlier one still in flight, even if the earlier one's settings read resolves last. `settings/store.ts`'s `setWorkspacePath` is the sole caller: `prepareOutgoing` rebinds the outgoing Android grant if the folder picker already reseeded the synthetic `/workspace` path, drains any queued or in-flight workspace-settings write and every operation `tauriBridge.ts` is currently tracking (see below), then clears tabs; `connectIncoming` activates and validates the new grant (a `listDir` probe, since a successful `restoreWorkspaceAccess` alone does not prove an expired Android grant is actually still readable) before `loadIncoming`/`publishIncoming` load and commit the new workspace's settings, including audit follow-up F-008's `workspaceSettingsCorrupted` signal, set or cleared from the incoming workspace's own decode result, never carried over from the outgoing workspace. `publishFailure` fails the transition closed to a recoverable inactive state rather than leaving stale UI active.

Two other modules participate as drain points the coordinator can wait on:

- `src/workspace/saveCoordinator.ts` (per-`{session, path}` debounced note-content writes) owns `prepareForTransition(session)`: it blocks new edits for that session, cancels its pending debounce timers, and awaits any already-invoked in-flight write before resolving. Each save entry also carries the workspace root needed by the containment-aware write path, so the coordinator never reaches back into current settings to decide where an older queued save belongs.
- `src/workspace/tauriBridge.ts` wraps workspace-facing bridge functions in `trackWorkspaceOperation`, which counts operations currently in flight; `drainWorkspaceOperations()` resolves once that count returns to zero. This is what lets `prepareOutgoing` wait for an in-flight rename, delete, or contained autosave to finish before an Android grant change could orphan it.

Other stores (`bookmarks/store.ts` today) register a reset callback with `workspaceTransitions.registerReset`, invoked once per transition right after outgoing work drains and before the incoming grant activates, so stale workspace-scoped UI state cannot survive into the new workspace's identity.

## Desktop shell: Tauri

- `src-tauri/src/lib.rs`: registers the Tauri plugins in use (`tauri-plugin-dialog` for the folder picker, `tauri-plugin-fs`, `tauri-plugin-deep-link` and `tauri-plugin-single-instance` for the `leotheca://` automation URLs below, `tauri-plugin-clipboard-manager` for their "read current note" command) and the `#[tauri::command]` functions from `commands.rs` plus the no-replace mutation commands from `workspace_mutations.rs` as the invoke handler.
- `src-tauri/src/commands.rs`: the ordinary filesystem operations the desktop frontend needs (`list_dir`, `find_markdown_files`, `find_all_files`, `find_all_entries`, reads and writes, create, rename, trash/delete, the workspace-scoped `write/create/rename/delete_workspace_*` variants and their shared `resolve_within_workspace` containment resolver, and `workspace_stats`), with filesystem unit tests in the same module. `src-tauri/src/workspace_mutations.rs` contains the stricter create-new and rename-no-replace workspace mutations that must make the collision decision at the native boundary. This is real filesystem access (`std::fs`); the OS's own permissions are the only sandboxing for anything outside the workspace containment check above, since the desktop OS already lets an app read/write anywhere the user can.
- `src-tauri/tauri.conf.json`: `build.devUrl` (`http://localhost:1420`, only used by `tauri dev`'s hot-reload server) vs. `build.frontendDist` (`../dist`, embedded into a real build). Which one a given binary uses is decided by the Tauri CLI at build time, not by the cargo release/debug profile; **a standalone/distributable build must be produced with `npx tauri build` (add `--no-bundle` to skip the `.deb`/`.rpm`/AppImage packaging steps if only the binary is needed), never a raw `cargo build --release`**, which silently leaves `devUrl` wired in and produces a binary that only works while a dev server happens to be reachable. See `agent-log/CHANGELOG.md`'s session 18 entry for the incident this note exists because of.

### Local automation: `leotheca://` URLs

`src/app/automationCommands.ts` (pure `parseAutomationUrl`) and the effect in `App.tsx` that wires it up implement `ROADMAP.md`'s "Local Automation Commands": `leotheca://read-current-note` copies the active note's content to the clipboard, `leotheca://new-note?content=...` creates a note from the given (URL-decoded) text, and `leotheca://open-favorites` opens the favorites panel. No network involved, offline-by-design is unaffected: the OS's own URL-scheme dispatch is the entire transport, the same way a `mailto:` link works. Desktop receives these URLs through Tauri and Android receives them through Capacitor's App API.

On Windows and Linux, per `tauri-plugin-deep-link`'s own docs, the OS spawns a *new* process with the URL as a CLI argument rather than notifying the running instance directly; `tauri-plugin-single-instance` (registered first, with its `deep-link` feature) intercepts that relaunch and forwards the URL to the already-running window's listeners instead. This is a real, deliberate behavior change beyond just adding the two commands: as of this feature, Leotheca only ever runs one instance at a time (a second launch attempt focuses the existing window rather than opening another), which is also a hard requirement for `read-current-note` to work at all on these platforms, since a genuinely new second instance would have no "current note" to read.

**What's verified versus what isn't**: the command parsing (`automationCommands.test.ts`) and the note-creation/clipboard logic are unit tested. The plugin registrations and `tauri.conf.json`/`capabilities/default.json` wiring are grounded in the plugins' own documented API (not guessed), and the app builds and passes CI with them registered. What is **not** verified in an unattended cloud sandbox with no real desktop: whether double-clicking a real `leotheca://` link on a real Linux/Windows/macOS install actually reaches the app. Linux's `MimeType=x-scheme-handler/leotheca;` registration is only added to `flatpak/com.leonardschwier.leotheca.desktop` (the Flatpak target); Tauri's bundler does not offer a way to add this to the AppImage it generates (no `desktopTemplate`-style option exists for that target, unlike `.deb`/`.rpm`, which do support one but weren't customized here either, since neither is this project's named distribution channel per `CONSTITUTION.md`). A real AppImage install therefore will not have OS-level URL-scheme dispatch working out of the box without further, separate packaging work; this is a known, open gap for a future session, not an oversight to silently paper over.

## Mobile shell: Capacitor + custom native plugins

- `android/app/src/main/java/.../MainActivity.java`: registers the custom SAF plugin implementation below.
- `android/app/src/main/java/.../FolderAccessPlugin.java`: the mobile equivalent of the ordinary filesystem side of `commands.rs`. Android has no direct filesystem access to an arbitrary folder the user picks; the only supported way is the Storage Access Framework (SAF): the user grants access to a folder via the system picker, the app gets an opaque, persistable `content://` tree URI, and reads/listing plus the long-standing file operations go through `ContentResolver`/`DocumentFile`/`DocumentsContract` against that URI. No mature Capacitor plugin for persistable SAF folder access exists (Capacitor's own Filesystem plugin dropped that support since Android 11), hence a small bespoke plugin rather than a dependency.
- `android/app/src/main/java/.../WorkspaceMutationPlugin.java`: extends `FolderAccessPlugin` under the same Capacitor plugin name and adds create-new plus rename-no-replace operations with stable failure codes. It validates one-segment names, refuses a pre-existing sibling, rejects providers that silently uniquify a requested new name, and performs rename through `DocumentsContract.renameDocument`. This is the Android half of F-004's native mutation contract; the SAF tree grant remains the actual containment authority.
- `android/app/src/main/java/.../LeothecaNewNoteWidgetProvider.java` and `LeothecaFavoritesWidgetProvider.java`: declare the two offline home-screen widgets separately. Each provider owns one dedicated layout and one local deep-link action, so the Android widget picker exposes New note and Favorites independently without giving either widget direct workspace access or duplicating command logic.
- `src/workspace/capacitorBridgeImpl.ts`: SAF URIs are opaque, you cannot construct a child's URI from its parent's the way you can concatenate a path. This file maintains an in-memory `path -> URI` cache, populated as directories get listed, self-healing (walks down from the nearest cached ancestor, re-listing as needed) for paths not yet seen in the current process, e.g. right after an app restart. Successful or ambiguous directory rename, trash, and delete operations evict the affected complete cache subtree, rather than assuming a provider preserves descendant URIs across the mutation. It also splits a small amount of always-available app-private storage (`Directory.Data`, no permission prompt) for the tiny global config pointer file only, keeping every actual note under the SAF-backed synthetic `/workspace` root, via a path-prefix check (`isWorkspacePath`). Workspace creates and no-replace renames resolve their parent URI through this cache and then cross the dedicated native mutation contract rather than implementing collision policy in TypeScript.
- **A gotcha worth knowing**: a Capacitor `DocumentFile.createFile(mimeType, name)` call lets the OS append a canonical extension for the given mime type if the name doesn't already end in it. Passing `"text/markdown"` for every file (including this app's own non-`.md` files like `.leotheca/settings.json`) silently turned `settings.json` into `settings.json.md` on disk, and broke overwriting on every subsequent save. Fixed by using `"application/octet-stream"` (no canonical extension) whenever the exact requested name matters. See session 18's changelog note above for the analogous desktop-side trap; both are instances of "the platform quietly does something clever with file naming/URLs that breaks an assumption the code made."

## Frontend module map (`src/`)

| Module | Responsibility |
|---|---|
| `app/` | `App.tsx`, the root component and toolbar; the command palette (`CommandPalette.tsx`); the markdown help dialog. Owns cross-cutting UI state (which panel is open, view mode) as module-level Preact signals. |
| `workspace/` | The file tree, tab bar, tab state (`store.ts`), and `fileTreeStore.ts` (create/rename/delete/search operations, workspace-scoped). `tauriBridge.ts` and its two `*Impl.ts` files live here too, see above, as do `saveCoordinator.ts` (debounced note-content autosave) and `workspaceTransition.ts` (the authoritative transition coordinator), see "Workspace transitions" below. `fileOpenAuthority.ts` (audit follow-up N-002) is a small authority-generation guard `app/App.tsx`'s `handleOpenFile` calls before its own `readTextFile` await: it makes the newest open request win over an older one still in flight (latest-selection-wins) and additionally invalidates any request that started before a workspace transition began, by capturing `workspaceTransition.ts`'s own transition generation rather than `settings/store.ts`'s `workspaceSession` (which isn't bumped until a transition publishes, well after tabs are already cleared). Mirrors `fileTreeStore.ts`'s internal search-authority pattern for the same class of problem. |
| `editor/` | `MarkdownEditor.tsx` (CodeMirror 6 source mode, syntax highlighting, `[[` wikilink autocomplete, and local Tab-expanded snippets; F04 Phase 2 adds `headingLinkCompletions`, suggesting a note's own headings, via the shared scanner, after `[[Note#`/`[[#` is typed; F04 Phase 3c adds `blockLinkCompletions`, the same shape but for explicit block ids after `[[Note#^`/`[[#^` (`HEADING_LINK_TRIGGER`'s own `(?!\^)` is what routes a block query to this separate completion source instead); F06 Phase 3 adds an `insertRequest` prop, mirroring `reveal` but performing a normal, undo-tracked edit for the insert-heading-link action), `livePreview.ts` (Source-mode inline decorations for headings/emphasis/code/lists/wikilinks; F04 Phase 2 adds a dedicated heading-link decoration pass reusing `linking/wikiSyntax.ts`/`wikiResolver.ts`, styled resolved/missing-heading/ambiguous-heading/broken-note, a cross-note fragment verified at the note level only, the same disclosed scope as Preview below; F04 Phase 3c adds the identical treatment for block-link fragments, sharing the same `fragmentLinkRanges` exclusion set with the heading pass so the plain-wikilink regex pass never double-decorates either kind), `snippets.ts` (safe parsing and cursor-local expansion of workspace settings), `MarkdownPreview.tsx` (rendered preview; F04 Phase 1 adds heading-link rendering/status through `linking/wikiSyntax.ts`/`wikiResolver.ts` behind `WorkspaceSettings.headingLinksEnabled`, and same-note heading click-navigation reusing `outline/outlineNavigation.ts`'s reveal signal, the same mechanism `OutlinePanel` uses; F04 Phase 3a adds the same treatment for `^block-id` fragments, plus stripping a resolved block's own marker token from the rendered source via `stripBlockIdMarkers` so it is never shown as visible text, per spec section 7.3; F04 Phase 4a adds read-only `![[Note]]`/`![[Note#Heading]]`/`![[Note#^block-id]]` embeds: a same-note embed extracts and renders its target section synchronously from the note's own already-available source, a cross-note embed emits a `data-lt-embed-id` placeholder resolved by a dedicated effect once the target note's content has been read, and both share one rendering path (`renderEmbeddedMarkdownToHtml`); F04 Phase 4b replaces that phase's disclosed single-level-only narrowing with real recursion per spec 11.4: an `EmbedRecursionState` (depth, an ancestry chain of note+fragment identities already being expanded, a shared `EmbedBudget`) threads through every same-note extraction and every cross-note resolution, nested ones included, so a further `![[...]]` inside an already-embedded section expands up to depth 3, a duplicate target already in the ancestry chain shows "Embed cycle stopped" instead of recursing, and the shared budget (25 resolved instances, 1 MiB total bytes, both checked and decremented only on an actual successful resolution) shows "Embed limit reached" once exhausted; a nested cross-note embed discovered while resolving another cross-note embed's content appends to the exact same growing `crossNoteEmbeds` queue the top-level render started, so the existing bounded worker-pool effect keeps draining it without a second effect. F04 Phase 4b follow-up 2 closes spec 11.4's remaining "per-note load timeout" gap: `readEmbedNoteWithTimeout` races a cross-note embed's own `readTextFile` call against an `EMBED_LOAD_TIMEOUT_MS` implementation constant (8000ms, same footing as the depth/instance/byte-budget constants), independent of the pre-existing cancellation-on-newer-preview-generation mechanism, which only covers a *newer* preview superseding an in-flight one, not a single slow or hung read within an otherwise-current preview; a timed-out read shows the same "Could not read embedded note" placeholder a genuine read failure already used, since spec 11.4's own required-placeholder list has no separate text for this case. `ImageViewer.tsx`, and `frontmatterEdits.ts` (the lossless per-property frontmatter parser/editor backing the Properties panel; F09 Phase 1 reuses its `parseFrontmatterProperties`/`hasFrontmatterBlock` export for collection query indexing, not a second frontmatter parser; F04 Phase 4a reuses its new `frontmatterBodyStart` export, the same delimiter detection as `hasFrontmatterBlock`, to embed a whole note's body without its frontmatter, per spec section 11.1). A cross-note heading or block link's click passes a heading key or block id up through `onOpenFile`; `App.tsx`'s `handleOpenFile` resolves it against the target note's freshly-read content and batches the tab switch with the reveal request so `MarkdownEditor`'s reveal effect never fires against the previous note's still-displayed content. |
| `linking/` | `store.ts`: extracts whole-note wikilinks, frontmatter aliases, tags, (F02 Phase 1) GFM task-list items, (F03 Phase 1) each note's structured wikilink records and headings (`LinkIndex.wikiLinksByPath`/`headingsByPath`, from the same per-note read, cache version 6), and (F09 Phase 1) each note's filesystem modification time plus its parsed top-level frontmatter properties, while building the shared workspace index. It owns note-level resolution and backlink computation. `BacklinksPanel.tsx` renders backlinks. See "The link index" below for its background-work and caching constraints. `wikiSyntax.ts` (F04 Phase 1, `spec/f04-heading-block-links-embeds.md`) is the one structured wikilink syntax parser every consumer must share (`[[Note]]`, `[[Note\|Label]]`, `[[Note#Heading]]`, `[[#Heading]]`, a same-note or cross-note `#^block-id` fragment, and, since F04 Phase 4a, a leading `!` immediately before `[[` marking `record.kind: "embed"` rather than `"link"`, note/fragment parsing and resolution otherwise identical between the two); it also exports `escapeWikiLinkText` (F04 Phase 2), the exact inverse of `unescapeWikiLinkText`, used when inserting a selected heading's own text into a completion, and `serializeWikiLink` (F06 Phase 3), the one allowed way to build fresh `[[Note#Heading]]`/`[[#Heading]]` link text for `outline/headingLinkActions.ts`'s copy/insert actions rather than hand-concatenating it. `wikiResolver.ts` maps a parsed record to a note path plus, for a heading fragment, a `HeadingRecord` from the shared scanner (`markdown/headings.ts`), including spec section 5.3's legacy-fallback compatibility check for a pre-existing note name that itself contains a raw `#`/`\|`; F04 Phase 2 reuses it unmodified from both `editor/livePreview.ts`'s Source-mode decoration and `editor/MarkdownEditor.tsx`'s heading completion. F04 Phase 3a adds `resolveBlockFragment`, the same case-insensitive-match/missing/ambiguous shape as `resolveHeadingFragment` but against `../markdown/blocks.ts`'s `scanBlockIds` (paragraphs in Phase 3a; Phase 3b adds list items and blockquotes; Phase 3d adds fenced code blocks, whose marker sits on its own separate line right after the closing fence rather than on the block's own last line, per spec 7.2's own explicit exception for that one kind; Phase 3e adds multi-line list-item/blockquote continuation, so a marker on an indented continuation line or a lazily-continued blockquote line now attaches to the item/quote it actually continues rather than being misread as its own standalone paragraph. A disclosed narrowing remains: headings are the one still-excluded spec-eligible block kind, and a loose list item's own blank-line-separated paragraphs, nested sub-lists/blockquotes, and tab-indented continuation are still not attributed, see that module's own doc comment and the `ROADMAP.md` follow-up). Since resolution and rendering both key only on a block's `key`/`kind`-agnostic id match, each new block kind works through the entire existing pipeline (same-note/cross-note link resolution, Preview status styling, Source-mode decoration and autocomplete, click navigation, and F04 Phase 4a's block embeds) with no changes needed anywhere outside `blocks.ts` itself, confirmed by dedicated `MarkdownPreview.test.tsx` tests rather than assumed. Note that `wikiResolver.ts`'s note-target resolution always reads the live `linkIndex` signal directly (via `resolveWikilink`), never an argument passed to it; any downstream consumer computing over a different `LinkIndex` snapshot (see `diagnostics/` below) must keep that snapshot assigned to the live signal for resolution to stay correct. |
| `graph/` | `layout.ts`: a self-contained Fruchterman-Reingold force-directed layout (no dependency), computed once per graph open rather than animated. `transform.ts`: the pan/zoom/hit-testing math (screen-to-world conversion, "zoom toward cursor", node hit-testing), pulled out of the component so it's plain numbers in, plain numbers out, no canvas or DOM. `GraphView.tsx`: a full-screen canvas overlay built on top of those two, imperative `draw()` calls (not Preact-render-triggered, canvas doesn't benefit from a virtual DOM diff), Pointer Events for unified mouse/touch pan/zoom. |
| `canvas/` | `canvasDocument.ts`: a versioned, tolerant `.canvas` document format (audit follow-up F-010). `decodeCanvas` keeps every top-level field it doesn't recognize and every node/edge array entry that isn't a valid `CanvasNode`/`CanvasEdge` but is still a real JSON object, so an edit to one card can never silently discard another card (or a future app version's own field) it doesn't understand; only a document that can't be parsed as an object at all, or whose `nodes`/`edges` field is present but not an array, is treated as unrecoverable (`null`), the same "missing isn't corruption, present-and-wrong-shaped is" distinction `settings/decode.ts` applies to persisted settings. `resolveCanvasFileReference` resolves a card's `filePath` against the canvas file's own directory through `workspace/paths.ts`'s `resolvePathWithinWorkspace` (the same containment contract audit follow-up F-004 established for the native write boundary), rather than trusting it as an already-absolute, unchecked path; a legacy absolute in-workspace path (the only shape ever written before this contract existed) is still accepted, but only once validated against the same containment boundary. `CanvasView.tsx` is the component: spatial cards, drag-to-move, click-to-connect edges, and local file-reference cards that call back into `App.tsx`'s `handleOpenFile` with an already-resolved path. |
| `outline/` | F06's note outline and heading breadcrumbs (`spec/f06-note-outline-heading-breadcrumbs.md`). `../markdown/headings.ts` is the shared, structure-scanner heading parser; `useNoteHeadings.ts` wraps it in a debounced, generation-guarded hook shared by `OutlinePanel.tsx` (the collapsible, filterable outline sidebar) and `HeadingBreadcrumbs.tsx` (the note-root-to-current-heading trail above the editor). `outlineActiveSection.ts` computes the active heading and ancestor chain from a plain source offset. `outlineNavigation.ts` is the shared reveal request `MarkdownEditor`'s `reveal` prop consumes to move the CodeMirror selection without remounting. `splitAuthority.ts` is a small pure reducer (`nextSplitAuthority`) deciding whether Source or Preview should drive the breadcrumb trail while Split view shows both at once (spec section 7.5); `App.tsx` owns the actual `useState`, feeding it a `source-cursor` event on every real Source cursor/keyboard action, a `preview-interaction` event on every real, direct Preview scroll/click/keydown (`MarkdownPreview`'s `onDirectInteraction`, deliberately separate from its `onActiveHeadingChange`, which also fires for a non-interactive recompute), and a `note-changed` event whenever the active note changes. `outlineVirtualization.ts` holds Phase 4a's pure, DOM-free windowing math (`flattenVisibleRows`, `computeVirtualWindow`) for notes above `LARGE_OUTLINE_THRESHOLD` (500) headings; `VirtualizedOutlineList.tsx` is `OutlinePanel.tsx`'s alternate renderer for that case (a flat, windowed list in its own scroll container instead of the nested `<ul>` tree), and `OutlineRowContent.tsx` is the row markup shared between both renderers so they cannot visually drift apart. Phase 3 (spec section 9) added `headingLinkActions.ts` (builds `[[Note#Heading]]`/`[[#Heading]]` text through `linking/wikiSyntax.ts`'s `serializeWikiLink`, copies it to the clipboard, or raises a new `outlineNavigation.ts` insert-request signal that mirrors the reveal request but performs a normal, undo-tracked `MarkdownEditor` edit through its new `insertRequest` prop) and `HeadingLinkActions.tsx` (the Copy link/Insert link row-action UI both `OutlineRowContent.tsx` and `HeadingBreadcrumbs.tsx` render for a heading, the two surfaces the spec requires this be reachable from, so their behavior and disabled-state reasoning never drift apart); a heading is disabled from linking when its text is empty or when it duplicates another heading's normalized text in the same note (reusing `OutlinePanel.tsx`'s own `computeDuplicateFlags`), since either case would make the resulting link resolve ambiguously. Ambiguous-note-basename path-qualification and the duplicate-heading "create a stable block link" flow (spec sections 9.2-9.3) are deferred: they need F04's resolver-ambiguity and explicit block-ID features, neither landed yet. |
| `bookmarks/` | Saved shortcuts to files or searches, persisted per workspace. |
| `tags/` | Extracts inline and frontmatter tags, builds the nested tag tree, and renders the Tags panel. It consumes `linking/store.ts`'s shared tag indexes instead of walking or reading the workspace independently. |
| `tasks/` | F02 (`spec/f02-workspace-task-hub.md`, see `ROADMAP.md` for the Phase 1/Phase 2 split): `TaskHubPanel.tsx` is the sidebar panel listing every GitHub-Flavored-Markdown task-list item found across the workspace, with status/path/tags/text filters and note/folder/none grouping (Phase 2). `../markdown/tasks.ts` is the shared, structure-scanner task-item parser (`scanTasks`), the second module of that infrastructure after `../markdown/headings.ts`; `linking/store.ts`'s `rebuildLinkIndex` calls it in the same per-note read already used for wikilinks/aliases/tags, populating `LinkIndex.tasksByPath`, so the panel is a projection of the existing workspace metadata index, not a second task database or a second workspace walk. `taskQuery.ts` is the pure filter/sort/group pipeline (`queryTasks`) over that index, with no file I/O of its own. `taskMutation.ts` is the conflict-checked toggle-complete write (`toggleTaskCompletion`): for a note open in a tab it applies the flipped marker through the same `updateTabContent` + `SaveCoordinator` path a keystroke edit uses, so a toggle and an autosave for the same path are never two independent writers; for a closed note it re-reads the file immediately before writing, serialized per path against a second concurrent toggle in the same note. Both paths re-scan the freshest available content and require an exact `TaskRecord` match before writing (the note or the workspace session moved on since the toggle started otherwise fails closed with no write), and on success incrementally replace just that one path's entry in `LinkIndex.tasksByPath` rather than triggering a full `rebuildLinkIndex`. Selecting a task row's label opens its note and reveals its exact source location by reusing `outline/outlineNavigation.ts`'s reveal-request signal, the same mechanism `OutlinePanel.tsx` uses, rather than a second navigation mechanism. Due-date filtering/grouping/sorting, "recently modified note" sorting, and persisted `TaskHubSettingsV1` query state are not implemented; see the F02 Phase 2 `ROADMAP.md` entry for why. |
| `diagnostics/` | F03 Phase 1 (`spec/f03-link-integrity-refactor-center.md`, narrowed to a first slice, see `ROADMAP.md`): `diagnostics.ts`'s `computeWorkspaceLinkDiagnostics` is a pure function over `linking/store.ts`'s `LinkIndex` (specifically its `wikiLinksByPath`/`headingsByPath`) classifying every wikilink as resolved, broken, missing-heading, or ambiguous-heading, reusing `linking/wikiResolver.ts` rather than a second resolution implementation; no second workspace walk. `DiagnosticsPanel.tsx` is a read-only sidebar panel listing every non-resolved finding, following `tasks/TaskHubPanel.tsx`'s structural convention (toolbar toggle, panel replacing the file tree, click-to-navigate via `outline/outlineNavigation.ts`'s reveal signal). Previewed rename/move refactoring, per-file diffs, a recovery journal, and rollback (the rest of F03) are F03 Phase 2, not built here. |
| `settings/` | `store.ts` (workspace path, theme, view mode, tab restoration), `workspaceSettings.ts` (the `WorkspaceSettings` shape persisted to `<workspace>/.leotheca/settings.json`), `globalConfig.ts` (F20 Phase 1, `spec/leotheca-workspace-profiles-sdd.md`: the app-wide pointer file, now a `GlobalConfigV2` catalog of `WorkspaceProfile` records — `id`/`name`/`icon`/`path`/`token?`/`lastOpenedAt` — plus `activeWorkspaceId` and `theme`, non-destructively decoded per section 18.3 (an unknown top-level or per-profile field round-trips via spread; a malformed profile record is dropped without discarding the rest of the catalog or any unrelated field, a disclosed narrowing of that section's fuller "retain for explicit repair" ask, see the ROADMAP entry); a legacy v1 document with no `workspaceProfiles` array and a non-null `lastWorkspacePath` is migrated in-memory into one profile (section 19.2) and persisted only after that workspace has actually opened), `workspaceProfiles.ts` (pure validation/sort/naming/locator-dedup helpers, sections 7.5/12/13.1, no signal reads), `SettingsPanel.tsx`, `WorkspaceSwitcher.tsx` (F20 Phase 1's minimal switcher — profile list, click-to-switch, Add workspace, Forget for a non-active profile — reachable from the app header, spec section 10.1 narrowed; search, arrow-key row navigation, command-palette entries, and a Settings-panel management section are deferred to F20 Phase 2). `store.ts`'s `activateWorkspaceProfile`/`addWorkspaceFromPicker`/`forgetWorkspaceProfile` are now the only profile-catalog entry points; `setWorkspacePath` (the pre-existing N-001/N-003 transition executor, unchanged in its own transition mechanics) takes an optional third `profile` argument so a successful transition can, in the same `afterPublish` step, mark that profile opened (bump `lastOpenedAt`, set `activeWorkspaceId`) and persist the *entire* current catalog through one `persistGlobalConfig()` (every global-config write in this module goes through it, so theme/recency/catalog edits always serialize the latest signal values together rather than each call site reconstructing a possibly-stale shape, section 18.4); a failed transition persists nothing, leaving the catalog exactly as it was (sections 16.4/17.2). `WelcomeDialog.tsx`'s and `SettingsPanel.tsx`'s own folder-picker flows both route through `addWorkspaceFromPicker` now rather than calling `setWorkspacePath` directly (section 20's "must no longer be called directly by UI surfaces"). |
| `collections/` | F09 Phase 1 (`spec/f09-smart-collections-property-views.md`, narrowed to a first slice, see `ROADMAP.md`): Smart Collections, saved queries over note metadata rendered as a read-only list. `collectionTypes.ts` is the query AST (`QueryNodeV1`/`QueryGroupV1`/`QueryClauseV1`) and storage schema (`SmartCollectionV1`/`CollectionsFileV1`), implementing the full nested-group, every-field, every-operator model the spec describes, not a narrowed subset. `collectionDecode.ts` runtime-decodes `<workspace>/.leotheca/collections.json` following `settings/decode.ts`'s established `DecodedField`/`corrupt` style; an invalid collection or clause is dropped without discarding valid siblings. `collectionTypesInference.ts` infers a frontmatter scalar's type (boolean/number/date/date-time/string) from `editor/frontmatterEdits.ts`'s already-parsed value and quoting style. `collectionQuery.ts` is the pure evaluator, operating only on `NoteRecord`s built from `linking/store.ts`'s shared `LinkIndex` (never a note body read). `collectionStore.ts` handles load/save/CRUD through the existing workspace-file bridge, following `bookmarks/store.ts`'s conventions. `CollectionsPanel.tsx`/`CollectionBuilder.tsx`/`CollectionResults.tsx` are the sidebar UI, following `TagsPanel.tsx`/`TaskHubPanel.tsx`'s conventions. Table/card views, sort-key configuration, live-preview polish, and in-view property editing are explicit follow-up scope, see `ROADMAP.md`'s F09 entries. |
| `styles/` | `theme.css`: CSS custom properties for the light/dark palette, bundled fonts. |

## The link index: shared workspace metadata

Most of the app is request-response (open a file, read it; save a file, write it). The workspace metadata index is the exception: backlinks, aliases, tags, tasks, link diagnostics, and the graph need information from every note. `rebuildLinkIndex` (`src/linking/store.ts`) starts with one native recursive walk, then builds the note-name, alias, tag, task, wikilink-record, heading, and backlink maps together. The Tags panel, the Task Hub panel, and the Link Diagnostics panel each consume that same index rather than performing another full-workspace read.

On a large workspace, especially over Android's SAF, filesystem work has to be bounded. The native walk avoids one bridge round trip per directory. A rebuild yields one paint frame before it starts, so the direct folder listing can appear first, and only the newest workspace rebuild may publish results. Note reads are limited to eight concurrent operations, and `linkIndexBuilding` lets the UI show progress. The index retains note metadata only when the native layer supplies a trustworthy modification time: `<workspace>/.leotheca/link-index-cache.json` persists the versioned cache across restarts, and a missing, malformed, or untrustworthy timestamp always causes a real read instead of risking stale results. The file contents themselves are never cached for display. The remaining cost of a fresh full directory walk is tracked in `ROADMAP.md`.

## Data and storage model

- A note is a plain `.md` file with YAML frontmatter (`created` timestamp, nothing else stamped automatically). Nothing about the file format is proprietary to this app; any text editor can open it.
- Per-workspace app state (sort order, font size, accent choice, snippet definitions, default view mode, delete behavior, and the last session's open tabs) lives in `<workspace>/.leotheca/settings.json`, inside the workspace folder itself, so the folder stays self-contained and portable, the same way a project-local config directory works. Snippets are deliberately simple tab-separated `trigger<TAB>replacement` lines: typing `;trigger` then Tab replaces only the text immediately before the cursor, and malformed definitions are ignored instead of affecting editing.
- Deleted files move to `<workspace>/.trash` by default (configurable per workspace to permanent deletion instead), never a hard delete unless the user opts in.
- The one piece of state that is *not* inside a workspace folder is the tiny global pointer (`globalConfig.ts`): which workspace to reopen on launch, the app-wide theme preference, and (Android only) the opaque SAF token needed to reconnect to that workspace's URI after a restart. This lives in Tauri's app config directory on desktop and a small app-private storage area on Android, never inside any workspace.
- Audit follow-up F-008: `settings.json`, the global config, and `bookmarks.json` are runtime-decoded, not just `JSON.parse(...) as Type` cast. `settings/decode.ts` has small, reusable field decoders (string/enum/number-range/boolean/string-array/relative-folder/array-dropping-invalid-entries) that resolve to a safe default and flag a field `corrupt` when it is present but doesn't match its type, as opposed to merely missing, which is not corruption. `workspaceSettings.ts`'s `decodeWorkspaceSettings` applies these to every field, validates `lastOpenPaths`/`lastActivePath` against the workspace root via `workspace/paths.ts`'s `isPathWithinWorkspace`, and preserves unknown top-level fields (including an unrecognized `version`) through a decode/re-save round trip instead of reconstructing the object from a fixed field list, so a genuinely newer app version's own settings aren't silently discarded by an older build. `settings/store.ts`'s `workspaceSettingsCorrupted` signal is set on a corrupt load and stays set, without triggering any write, until the user explicitly clicks `SettingsPanel`'s "Rewrite settings file" (`repairWorkspaceSettingsFile`): a malformed file is never silently overwritten by the defaults loading it produced. `globalConfig.ts` and `bookmarks/store.ts` get the same per-field/per-entry validation without a dedicated recovery UI, since neither is ever auto-saved merely by loading (see `ROADMAP.md`'s F-008 entry for the full reasoning). The link-index cache (see "The link index: shared workspace metadata" above) is deliberately not part of this: it overlaps the active F-012 claim's own work on that file.

## Build and distribution architecture

| Platform | Build tool | Artifact(s) | Status |
|---|---|---|---|
| Linux desktop | `npx tauri build`, then the bundled appimagetool plugin packages its generated AppDir | AppImage, `.deb`, `.rpm` (configured in `tauri.conf.json`'s `bundle.targets`) | The release workflow packages the validated AppDir directly to avoid a linuxdeploy finalization failure; Flathub manifest complete with generated dependency source lists, not yet test-built, see `flatpak/README.md` |
| Android | Gradle via Capacitor (`npx cap sync android && ./gradlew assembleDebug`/`assembleRelease`) | APK | Dev APKs use the repository's fixed `android/debug.keystore` so CI artifacts can update each other. This public debug key is not a release signing credential. F-Droid packaging blocked on an open question about offline npm builds, see `packaging/f-droid/README.md` |
| macOS | Not yet built | Planned: Homebrew | Confirmed intended platform (see `CONSTITUTION.md`'s Decisions Log), no build target exists yet |
| Windows | Not yet built | Planned: direct download from GitHub Releases only, no package manager/store | Confirmed intended platform, no build target exists yet |

The root `VERSION` file is the single source of truth for the project's semantic version (audit follow-up F-015): `package.json`, `src-tauri/Cargo.toml`'s `[package]` version, `src-tauri/tauri.conf.json`, the web-clipper extension manifest, Android's `versionName`, the Flatpak AppStream metainfo's most recent `<release>`, and the F-Droid draft metadata's version fields are all validated against it, never generated from it, by `scripts/versionConsistency.js` (pure comparison logic, unit-tested in `scripts/versionConsistency.test.js`) and its `scripts/checkVersion.js` CLI entry point, run in CI as `npm run check-version`. Android's `versionCode` is derived deterministically from `VERSION` (`major*10000 + minor*100 + patch`) rather than hand-maintained, so it can't accidentally regress or duplicate a previous release. Distributable build metadata that names a source ref (currently only the F-Droid draft's `Builds[].commit`) must reference an immutable tag or full commit SHA, never a moving branch; `scripts/versionConsistency.js`'s `isImmutableBuildRef` enforces that. Separately, `.github/workflows/release.yml`'s `version-guard` job checks, only on a real `v*` tag push, that the tag equals `v` plus `VERSION`'s contents and that `CHANGELOG.md` already has a matching versioned heading, gating `publish` the same way the `validation` job does.

`.github/workflows/ci.yml` is the canonical validation contract for a commit. It is both the normal push/pull-request workflow and a reusable workflow called directly by `.github/workflows/release.yml`, so release publication depends on validation of the exact caller commit rather than a separate run that could describe another SHA. Its required jobs cover TypeScript checking, lint, frontend tests and production build; Rust formatting, Clippy with warnings denied, tests and `cargo check`; Android unit tests and a debug APK build; an Android emulator install of that APK; and an AppImage build, extraction, and headless process-launch smoke test. A final `validation` job runs with `always()` and succeeds only when every required job result is `success`, making skipped, cancelled, or failed prerequisites non-authoritative.

The artifact smoke tests are intentionally narrower than physical-device or full desktop verification. The Android check proves that the produced debug APK installs and is registered in an emulator; it is not physical-device, SAF picker, touch, or large-vault verification. The AppImage check proves that the produced artifact extracts and its packaged application process remains alive under a headless X11 display long enough to count as a launch smoke; it does not prove pixels rendered correctly on a real desktop, Wayland behavior, URL-handler registration, or Fedora graphics compatibility. Those environment-specific checks remain manual/open where the roadmap says they do.

## Testing strategy

- **Rust**: unit tests alongside each command in `src-tauri/src/commands.rs` and `src-tauri/src/workspace_mutations.rs` (filesystem operations against real temp directories, not mocked).
- **Frontend, pure logic**: Vitest, on the default Node environment for logic that doesn't need a real DOM/canvas: the wikilink extraction and link-index build (`src/linking/store.test.ts`, including a test that directly asserts the bounded-concurrency fix caps in-flight reads rather than trusting the implementation), the graph layout and pan/zoom/hit-testing math (`src/graph/layout.test.ts`, `src/graph/transform.test.ts`), workspace settings load/save/merge behavior (`src/settings/workspaceSettings.test.ts`), and the `[[` wikilink autocomplete's completion source (`src/editor/MarkdownEditor.test.ts`). This last one looks like it would need a real DOM/CodeMirror view but doesn't: `CompletionContext` is a plainly-constructible class given just an `EditorState`, no view or DOM required. Most other test files opt into a `jsdom` environment instead via a per-file `/** @vitest-environment jsdom */` docblock, either because the module under test transitively reads `window.matchMedia`/`document` at load time (`src/settings/store.ts` does, for system-theme detection), or because the file renders a component with `@testing-library/preact`, which needs a real DOM. This is a deliberate per-file choice rather than a global config change, so the pure-logic files above stay on the faster default. `settings/store.test.ts` caught a real bug the day it was added: `closeAllTabs` did two separate signal writes instead of one atomic one, see `agent-log/CHANGELOG.md` session 23.
- **Frontend, components**: `@testing-library/preact` (`render`/`fireEvent`/`cleanup`, plus Vitest's fake timers for anything debounced) for component-level interaction bugs that live in the DOM/event layer itself, not in a store module underneath it. Now the majority of interactive components have a test file this way (file tree, tabs, context menus, name/rename prompts, the command palette, the settings panel, the markdown preview's sanitization and wikilink click-through, the image viewer's async-race guard, and the smaller dialogs), each following the same two patterns: (1) when a component reads module-level signals directly rather than props, mock only the store modules that have their own module-load side effects the test doesn't want (typically `../settings/store`, for its `window.matchMedia` call) and let the rest of the real store run, driving the real signals directly from the test rather than re-mocking state that's already simple to set up for real; (2) mock a component's own store dependencies with `vi.mock`, using real `@preact/signals` `signal()` instances in the mock (not just spies) when the component needs to react to changes, so the component's actual reactive read/write behavior is exercised rather than assumed. Let genuinely-nested child components render for real when they only depend on already-mocked modules, rather than mocking every child too. **Always call `cleanup()` in `afterEach`**: `@testing-library/preact` does not auto-cleanup between tests, forgetting it leaves every previous test's render sitting in the DOM and produces confusing "multiple elements found" failures in the *next* test that look like an app bug but aren't (session 31 hit this on the first draft of `CommandPalette.test.tsx`). Several real, previously-unnoticed bugs have been found this way: a debounced search race on workspace switch (`Sidebar.test.tsx`), a stale-state bug in the file tree's expand/selection tracking after rename/delete (`fileTreeStore.test.ts`), a modal backdrop click closing more than it should (`SettingsPanel.test.tsx`), and the file tree's "selected" highlight tracking the wrong signal (`FileTree.test.tsx`). Each was fixed following the same revert-confirm-restore discipline: temporarily undo the fix, confirm the new test fails with the expected assertion error (not vacuously), then restore and confirm green again. See `agent-log/CHANGELOG.md` for the specifics of each.
- **Manual, on-device**: UI-heavy code that isn't reasonably unit-testable even with the above (the graph view's actual canvas *pixel* rendering and on-screen appearance, SAF folder picker flows, touch interactions) is verified live against a real device and a real, large vault where possible, not just fixtures; see recent `agent-log/CHANGELOG.md` entries for what's been confirmed this way versus what's still owed. The graph view's *interaction* logic (double-tap-to-open, drag vs. pinch-to-zoom branching, the close button, the empty-vault state) turned out testable despite the canvas involvement: jsdom's `HTMLCanvasElement.getContext("2d")` returns `null` rather than throwing, and `GraphView.tsx`'s own `draw()` already no-ops on a missing context, so `GraphView.test.tsx` stubs `ResizeObserver` and the Pointer Capture methods (neither implemented by jsdom) and mocks `computeLayout` for deterministic node positions, without needing a canvas-mocking library.

## Where to go next

- [`ROADMAP.md`](../ROADMAP.md): what's shipped, what's still open, in what order.
- [`CONSTITUTION.md`](../CONSTITUTION.md): binding rules, product principles, the Decisions Log (why past architectural choices were made).
- [`CONTRIBUTING.md`](../CONTRIBUTING.md): how to get a dev environment running and what checks a change needs to pass.