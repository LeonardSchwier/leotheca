# Session log — hermes-local-20260921T152252Z-4ac9985d

## Summary
Fixed rm-9d2a9eb41e719983: `saveCoordinator.flush()` was bumping the revision counter before writing, creating a phantom unsaved-work state. The fix writes at the current revision instead, matching `prepareForTransition`'s correct behavior.

## Work done
1. Read CONSTITUTION.md, skills (autonomous-roadmap-delivery, roadmap-workflow, verification-suite, pre-push-verification), AGENTS.md.
2. Found item rm-9d2a9eb41e719983 in ROADMAP.md (Open Bugs, claimed by prior session hermes-local-20260921T150604Z-3852b515).
3. Analyzed `saveCoordinator.ts`: `flush()` called `++entry.revision` before `writeRevision`, inflating the revision counter. `writeRevision` sets `savedRevision = revision` on success, so the net effect was masked in simple cases, but the bump was semantically wrong and created issues for never-edited notes (revision 0 → 1, spurious write).
4. Implemented fix: `flush()` now writes at `entry.revision` (no bump) and returns early if `revision === 0`.
5. Added 6 regression tests covering: clean flush, pending-edit flush, error path, no-op (never-edited), in-flight race, retry invariance.
6. Verified regression: reverted fix → 1 test fails; restored fix → all pass.
7. Full verification suite: tsc ✓, lint ✓, check-version ✓, 146 test files / 2771 tests ✓, build ✓, git diff --check ✓.
8. Committed (2811c39), pushed to agent branch, CI green (5/5 jobs).
9. Fast-forward merged to main (0e505a3), updated ROADMAP.md (item → Implemented), pushed to main.
10. Released claim via ledger.

## Key decisions
- `flush()` is a sync mechanism, not a new save attempt — it should write at the current revision.
- `retry()` intentionally still bumps revision (it represents a deliberate "try again").
- The `revision === 0` guard prevents spurious writes for never-edited notes.

## Landed
- SHA: 0e505a3 on main
- CI: green on agent branch (run_id=35619584821, 5/5 jobs); main push CI in progress (ROADMAP-only change)
- Handoff: .agents/handoffs/rm-9d2a9eb41e719983.md
