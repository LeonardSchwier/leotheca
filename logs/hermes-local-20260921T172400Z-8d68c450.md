# Session Log: hermes-local-20260921T172400Z-8d68c450

**Date:** 2026-09-21
**Duration:** ~30 minutes (within 90-min watchdog)
**Status:** Complete — one bug fixed, landed, and released

## Summary

Identified and fixed a bug in `src/capture/captureCommit.ts` where attachment filenames contained colons (invalid on some filesystems) and used UTC time instead of local time (violating F05 §9.3). Added a regression test. All checks green. Landed on main.

## Work performed

1. **Reviewed recent commits** to identify actionable maintenance work.
2. **Identified the bug:** `generateAttachmentFilename` (line 122) and `createNoteWithTitle`'s timestamp (line 331) both used `toISOString().replace(":", "-")` which:
   - Only replaces the first colon (`.replace` without `/g` flag)
   - Uses UTC time instead of local time
3. **Added roadmap item** `rm-ef110d3948003207` to ROADMAP.md.
4. **Claimed the item** via `scripts/agent_ledger.py` from the control worktree.
5. **Implemented the fix:** Replaced `toISOString()` with explicit local-time component extraction in both functions.
6. **Added regression test:** Verifies the filename matches the expected pattern and contains no colons.
7. **Ran all verification checks:** tsc, eslint, vitest — all green.
8. **Merged to main** and **finished the claim** in the ledger.

## Artifacts

- **Roadmap item:** rm-ef110d3948003207 (done)
- **Handoff:** `.agents/handoffs/rm-ef110d3948003207.md`
- **Landed SHA:** 43399c1 (merge commit)
- **Work branch:** agent/rm-ef110d3948003207/5b9b0c9ac74c

## Notes

- Network was flaky (proxy 500 errors) — several retries needed for push/fetch.
- The full test suite has 1 pre-existing unhandled error in an unrelated agent's work tree (not caused by this change).
- All 22,097 tests pass.
